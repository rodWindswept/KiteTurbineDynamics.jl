# Tickets — 5 kW re-run correctness work (2026-09-02)

Parent plan: `docs/plans/2026-09-02-mass-model-audit.md` (§9 step-by-step).
DECISIONS entry: `DECISIONS.md` [2026-09-02].

> Note: GitHub Issues could not be published from this session (`gh` is
> snap-confined and cannot run here).  This file is the tracking artifact;
> publish these as issues on a credentialed machine, in the order listed.

## T1 — Fix the mass model (min 2 mm wall, per-ring sum, ring knuckles)
- **Type:** AFK.  **Blocked by:** none.
- **What:** make `expansion_airborne_mass` return the true airborne mass:
  (1) no tube wall thinner than 2 mm; (2) ring mass summed per-ring from the
  actual radius profile, not one averaged ring; (3) every ring→cable knuckle
  counted with one shared rule.
- **Files:** `src/expansion_analysis.jl`, `src/objective_evaluator.jl`
  (`build_system_from_v10`), `src/trpt_optimization.jl`, new test.
- **Acceptance:** island-1 winner's no-lifter mass jumps materially from
  ~4.6 kg; a unit test pins the per-ring sum vs the old average; fast suite
  green.

## T2 — Apply wake blocking in the settle step
- **Type:** AFK.  **Blocked by:** none.
- **What:** the cold-start settle equilibrium scan
  (`settle_to_operational_state`) must use the same 0.75× wake de-rate as the
  ODE (`sys.rotor.wind_factor`, `er.wind_factor`), so multi-rotor machines
  start at the correct spin speed instead of overshooting.
- **Files:** `src/initialization.jl`, new test.
- **Acceptance:** a multi-rotor system's settle ω matches its blocked
  equilibrium; a unit test pins it; fast suite green.

## T3 — Fitness: appropriateness + safety penalties
- **Type:** AFK.  **Blocked by:** none.
- **What:** replace/augment `mass_min_fitness` so "good" also means: penalise
  P > 5 kW, penalise approaching overtwist, penalise high beam utilisation
  (FoS close to the 2.5 floor).  Keep the hard reject gates (FoS < 2.5,
  P < 5 kW).
- **Files:** `src/objective_v12.jl`, new test.
- **Acceptance:** unit tests for each penalty; the new fitness ranks a
  slightly-over-powered or margin-thin design worse than a well-margined one;
  fast suite green.

## T4 — Safer seed genomes
- **Type:** AFK.  **Blocked by:** T1, T2, T3.
- **What:** re-derive starting designs that are safe and real (≥ 2 mm walls,
  comfortable FoS, non-degenerate line count) rather than the suspicious light
  corner.  Record why the previous seed failed.

## T5 — Re-run the 5 kW campaign
- **Type:** AFK.  **Blocked by:** T4.
- **What:** launch the 3-island rotorcount campaign on the corrected evaluator
  (runbook: `docs/plans/2026-08-27-campaign-launch-runbook.md`).

## T6 — Re-check winner + acceptance re-baseline
- **Type:** AFK.  **Blocked by:** T5.
- **What:** re-gate the winner, by-hand weight sanity check, then re-baseline
  the acceptance suite (`docs/plans/2026-08-22-acceptance-rebaseline.md`).

## Wave plan
- **Wave 1 (parallel now):** T1, T2, T3.
- **Wave 2:** T4 (after T1–T3).
- **Wave 3:** T5, then T6.
