# Instrument Trust Log

**Purpose:** Shared cross-instance truth source for both Hermes instances (laptop + desktop) working on KiteTurbineDynamics.jl.  Durable state lives version-controlled, not in the private memory of either instance.  Read at session start.

**Last updated:** 2026-09-21

## Duplicate-Representation Audit

Constants or parameters defined in two places that must stay in sync:

| Item | Location A | Location B | Risk | Action |
|------|-----------|-----------|------|--------|
| ODE timestep | `control_map_hunt.jl:37` `DT=4e-5` | `objective_evaluator.jl:92` `V11_DT=4e-5` | **DUPLICATE, in sync**. Two constants, same value, must not drift | Single source, or test asserting `DT == V11_DT`. **Worse than a duplicate (2026-09-16): the value itself is not generally stable, see the Fault Ledger row below** |
| ODE timestep, DERIVED | `initialization.jl` `stable_dt_for_system` | `objective_evaluator.jl:567` (uses it) | **FIXED 2026-09-16.** `objective_evaluator_ramp.jl` used the raw `V11_DT` for its chunk and window calls (`:115`, `:123`, `:147`, `:148`, `:161`, `:190`). It now derives `dt` once and scales every step count | Measured below: the raw step is non-finite within 0.02 s for the 5 kW build. Ramp path fixed in the same change |

---

---

## Fault Ledger

Every confirmed instrument fault with its fix commit, so neither instance re-discovers a fixed bug.

| Date | Fault | Symptom | Root Cause | Fix Commit |
|------|-------|---------|-----------|------------|
| 2026-09-22 | **A ring's attachment points sit on two planes**. TRPT lines use the synthetic tilted plane. Bridles use the fixed shaft plane, so one rigid ring gets two circles | `rope_forces.jl:269` resolves a ring attachment as `is_bridle ? shaft : tilt`, and `is_bridle` tests only `end_a`. Bridles are stored bearing-first, so a bridle's hub end always takes the shaft plane | Joining the planes raised island 1's cone mean load from 3.87 N to 1382 N. Its minimum rose from 0 to 74 N. Island 3 became perfectly steady at hub p2p 0.0000 m, with a steady 158.9 N cone and no slack | **FIXED and RULED (Rod, 2026-09-22): one ring plane is CANONICAL**, and the exception is PERMANENTLY RETIRED. The basis selection no longer inspects `is_bridle` at all. That also removed a reader/force-path disagreement, because `get_max_rope_tension` and the gate's slack tracker always read bridles on the tilted basis while the forces used the shaft basis. The tilt is itself synthetic and unanchored, and it applies to every ring. Restricting it to the hub ring made island 1 worse, so the global tilt is not the defect |
| 2026-09-22 | **Off-axis bridle wrap torque is a second independent driver** | `rope_forces.jl:359` projects the rope moment onto the shaft axis. For a bridle the attachment is out of the ring plane, so that moment is spurious. Clamping it for bridles kept island 1's cone alive. The minimum rose from 0 to 37.8 N over 2 s. | Toggle `bridge_axial_torque`, default unchanged. It is NOT additive with the plane fix. Bridletilt alone gives 86.8 N. Adding no-torque gives 87.3 N. The plane fix dominates |
| 2026-09-22 | **`design_axial_preload` omitted every expansion rotor's thrust**. | The profile added only ring weight down the shaft. So every segment below an expansion rotor was under-tensioned against the ODE force model (`ring_forces.jl:288-305`). The shortfall inflated that segment's twist demand. The L/r 2.0 fixture read demand 1.1276 and crossing 1.4716, and the floor refused it. The corrected profile reads 0.8671 and 0.8959, and the floor repairs it. This is the 2026-09-12 row below, left as **No code change**. | Each rotor's thrust now enters the segment below its own ring. The law is `S_{r-1} = S_r + F_r - W_r*sin(beta)`, so ring r's thrust goes in `F_ax[r-1]`. Section B `T_top` stays MAIN-ROTOR-ONLY, because the expansion rotors sit below that cut. Summing them into `T_thrust` raises every segment including the top ones. Measured: 14 pinned assertions went red in `test/test_trpt_realisability.jl`. Fixture re-baselined with the reasoning. Suite 2163/2163. `57b3f58`. |
| 2026-09-22 | **Expansion-rotor translational mass never reached the ring node**. | On island 1 rings 8 and 9 read `mass = 1.5702 kg`, the bare ring. They also carried `er.mass` 2.547 and 2.613 kg, and rotary inertia `I_z` 31.9055 and 32.4947 kg m2. Their lateral sensitivity `a = F/m` was about 2.6x too high. The rotary inertia `J_rotor` was added on 2026-08-25. The physical mass was not. | `mass_node += er.mass` in `_build_kite_turbine_system_impl`, always. It is deliberately NOT gated on `EXPANSION_PHYSICS[].blade_inertia`. The mass is real either way, and `expansion_airborne_mass` already budgets it for lifter sizing. So the ODE node weighed less than the lifter was sized against. `er.mass` is the ASSEMBLY total. Add it once, never times `n_blades` (`test_blade_mass_law.jl` guards that). Rings 8 and 9 now read 4.1175 and 4.1836 kg. Island 3 has no expansion rotors and is unchanged bit-for-bit. `57b3f58`. |
| 2026-09-22 | **`F_radial` pushed on the ring centre of mass**. | `ring_forces.jl` took the ring centre node's lateral offset from the shaft axis and applied `F_radial .* rad_dir` to `forces[ring_gid]`. For N symmetric blades the rim loads sum to zero over the attachment vertices. So the net centre-of-mass force is zero. The block destroyed the on-axis equilibrium and parked the ring at an offset `F/k`. Only expansion rings ran it, so it biased exactly the multi-rotor machines. Measured 2.715 N on ring 9 and 5.310 N on ring 8. That is a bias of order newtons, **not** a runaway force. | Block removed. `F_radial` remains a STRUCTURAL rim load, priced by the ring and FoS evaluator. **OPEN, logged and not fixed**. The designed radial restraint is the spoke spring at `ring_forces.jl:376-396`. It is inert in every ODE run behind the Item 4 verdicts. The `spoke` argument defaults to `nothing`, and neither `run_v13_5kw_masslift.jl:261` nor the wobble gate passes one. So the ring now has no radial restraint in those runs. **RULED INERT (Rod, 2026-09-22): that is the decision, not an oversight**. Only the ground ring and the back-line anchor touch the ground. A TRPT column is a free-floating tensegrity with no centreline anchor. Energising the spokes would add an artificial guide-wire pulling every ring toward the ground station's line of sight. Do not switch them on as a stability crutch. `57b3f58`, and the ruling is recorded in `ring_forces.jl`. |
| 2026-09-22 | **`backline_payout` validated then IGNORED. A dead trim knob, and a test pinned the no-op**. | `back_line_tension(d, trimmed, payout, EA)` used `payout` only in its `payout >= 0.0` guard. The tension law never read it. So the winch trim could not move the hard stop, though the docstring said "Payout is the winch trim: it moves the hard stop". `test_back_line_element.jl` asserted identical tension with and without payout. The comment above that assertion described the behaviour it was disabling. The back line is the altitude limiter, and it must run on its elastic travel. Measured on the pre-remediation build: island 1 settled exactly ON the 320 N stop. It had 0.000 m of the 0.800 m travel left, and it failed the gate. Island 3 settled 0.322 m INTO the travel and passed. | Paying line out lengthens the physical line, so the stop moves outward. The law uses `stop = trimmed_length + payout`. `payout = 0` reproduces the previous law bit-for-bit. Tension at the design distance is now `k_soft*(travel - payout)`. That is the knob which trades design-point tension for remaining compliance. `test_back_line_element.jl` now asserts the real trim semantics, 39/39. `57b3f58`. |
| 2026-09-22 | **`preload_thrust_budget.jl` triple-counts expansion thrust, so the "85 % of thrust" figure is 3x high**. | The probe prints `Fa` as "F_axial/blade" and then accumulates `er.n_blades * Fa`. But `expansion_rotor_forces` already returns the `n_blades` TOTAL (`expansion_rotor.jl:388`). With `n_blades = 3` every expansion figure is 3x high. So the recorded "+914.5 N and +820.5 N, 2044.3 N total, main rotor about 15 %" is not the model's split. The ODE is correct, because `ring_forces.jl` uses `F_axial` directly. Measured correctly on island 1: ring 8 +101.3 N, ring 9 +24.0 N. That is +125.3 N of 1537.3 N, so **8.2 %** expansion. | **Diagnostic fault, no model change**. The rule in `physics-topology.md` section 5 item 5 still stands, and `design_axial_preload` now enforces it. But do not re-quote the 85 and 15 % magnitudes, nor the "wrong by 6.6x" factor in the 2026-09-12 row. Corrected split in handover Rev 2. `57b3f58` records the fix. The probe is left as-is and flagged. |
| 2026-09-21 | **A reader that collects the bridle sub-segments before the settle reads stale rest lengths** | The stale value is the uncut design gap. It is **0.364 mm longer** than the cut rest length, so bridle tension reads far too low (a first probe read 54 to 68 N where the live path reads about 283 N). Every live reader disagreed with it, and the stale reader looked self-consistent | The settle runs `apply_design_bridle_preload!` (`initialization.jl:2203`, definition `:1317`). That function rebuilds the bridle sub-segments with preload-cut rest lengths (`L0 = gap / (1 + T_bridle / EA)`, cut applied `:1334/:1350`). A pre-settle collection holds the orphaned sub-seg objects, not the live ones | **Probe-level fault, no model change.** Fix: collect after the settle, or read the live fields on each call. Checks: `scratch/probe_bridle_reader_check_fixed.jl` (reproduces the live path exactly: L0 4.659849663 -> 4.659485227, T 283.103 == 283.104) and `scratch/diag_bridle_discrepancy.jl` (localiser). The gate matrix readings were never affected |
| 2026-09-16 | **`test_settle_validity.jl`'s `acc0` metric measured the machine's own spinning DRAG, not the handoff jerk. Corrected.** | `acc0` read **17 468 m/s^2 (1780 g)**, argmax a **`RopeNode` of 2.25 g** carrying 39.3 N. Zeroing ONLY the node TRANSLATIONAL velocities (`omega` retained) drops it to **284.5 m/s^2 (29 g)** with the argmax a **`RingNode` (0.836 kg, 238 N)**. So **98.4 %** of the reading was drag on nodes that are correctly spinning at `omega·r ~ 13.45 · 2.4 ~ 32 m/s` (settled `max|v|` = 32.2 m/s, rms 9.1; V1 already asserts the NON-rotational velocity is 0.0) | The test called `multibody_ode!` on the SETTLED state, whose velocity block IS the legitimate rigid rotation, so the aero drag and the rope material damper were LIVE. A 39 N drag force on a 2.25 g node reads as 1780 g, and **no equilibrium solver can remove it** — it is a correct operating-point force. This **CORRECTS the earlier attribution** in the `acc0` row below ("a rope-tension imbalance, not a rotor term"): the force is velocity-dependent, so it is drag/damping, not a static tension imbalance | **Metric corrected.** V6 now measures `static_acc0` (node translational velocities zeroed, `omega` retained — exactly the force path `ACTIVE.md` item 3 mandates). The assertion stays `@test_broken acc0 < 10 g`, so this is **not** a re-baseline; the real remaining defect is **238 N on `RingNode` 11 (29 g)**, a genuine target for the static solver. Probes: `scratch/diag_acc0_velocity.jl`, `scratch/diag_acc0_node.jl` |
| 2026-09-16 | **The ramp scoring path integrated at the raw `V11_DT` with no stability derating. `evaluate_ramp` diverged immediately for the 5 kW build** | Integrating the settled 5 kW seed from its settled state at `V11_DT = 4e-5` goes **NON-FINITE within 0.02 s**. At the derived step it is finite. `evaluate_ramp` is not a side path: `objective_v12.jl:280` (`objective_v12_warmstart`) calls it, and `test_objective_v12.jl` exercises it | Same as the test-suite dt fault below: `stable_dt_for_system` returns 2.040e-5 for this build (Lmin 0.293 m) against the raw 4e-5, a factor 1.96. `objective_evaluator.jl:567` already derived it; the ramp path was the only remaining ODE producer using the raw constant | **FIXED.** `objective_evaluator_ramp.jl` derives `dt` once and scales every chunk and window step count, so the simulated chunk (2 s) and window (60 s) durations are preserved. Fast suite 2139 pass / 0 fail / 1 broken, including the `objective_v12_warmstart` test. Probe: `scratch/diag_ramp_dt.jl` |
| 2026-09-16 | **Even at the stable step, the settled 5 kW seed grows to very large node accelerations within a second** | At the derived step the same state reaches max abs acc of **83 341 m/s^2 at 0.02 s** and **90 434 m/s^2 at 0.1 s** (plateauing there across the probes' windows). Earlier measurement on the same build at 0.04 s read 717 m/s^2, so this is continued growth rather than a new fault | Not the step size — this is the handoff-imbalance row below, viewed over a longer window. The settle does not produce a force-balanced state, so the transient grows instead of settling | **OPEN.** Part of the same static-solver item. Recorded so the dt fix is not mistaken for a cure: the ramp path no longer diverges, but the seed's start-up transient is still not settled. Probes: `scratch/diag_ramp_dt.jl`, `scratch/diag_kick_decay.jl` |
| 2026-09-16 | **The canonical `dt = 4e-5` is NOT a stable step for a fine-meshed build. Several acceptance tests integrated 1.96x over the linear stability limit** | Integrating the 5 kW seed from its settled state at `dt = 4e-5` gives max node acceleration **12 123 m/s^2**, rising **129 222** after one step, **4.9e6** by ten steps, then non-finite — i.e. it reads as a catastrophic start-up divergence. At the correct step it is **40.1 m/s^2**. `test_settle_drag_alignment.jl` (an ACCEPTANCE test) integrated its 20 s windows at the unstable step | `stable_dt_for_system` returns `min(dt_ref, dt_ref*sqrt(Lmin/L_ref)/margin)` = `min(4e-5, 4e-5*sqrt(Lmin/0.5)/1.5)`. Because of the 1.5 margin it equals `dt_ref` **only when `Lmin >= margin^2 * L_ref = 1.125 m`**, so `4e-5` is a ceiling for coarse geometry, never the stable step for a realistic mesh. The 5 kW taper builds `Lmin = 0.293 m` → **2.040e-5**. `settle_to_equilibrium` and the main evaluator derive it; tests and the ramp path did not | **FIXED `ae864a6`.** Every test dt is now derived (`test_settle_drag_alignment.jl`, `test_metric_consistency.jl`, `test_dashboard_smoke.jl`, `test_pitch_depower_sequence.jl`, `test_power.jl`), preserving each window and frame count. New static guard `test/test_dt_guard.jl` forbids a numeric dt literal in tests, with one documented allowlist entry (`test_bearing_alignment.jl`, where the 1 ms step is the SUBJECT). Acceptance converted file passes 9/9 in ~70 s vs ~35 s. **Still open:** `objective_evaluator_ramp.jl` uses raw `V11_DT` — a production path, not a test |
| 2026-09-16 | **The ODE handoff state from the settle is not in force balance, but the reported magnitude was inflated ~300x by the dt fault above** | `test_settle_validity.jl` reports `acc0` against a 10 g target. Reported as **12 123 m/s^2** (~1240 g), which is an artefact of the unstable step. At the correct `dt` it is **40.1 m/s^2** (~4.1 g), still 2.4x over target, and grows **40 -> 30 -> 135 -> 717 m/s^2** over 40 ms. The worst node is a `RopeNode` (mass 0.00297 kg) carrying **36.04 N** unbalanced, shared across neighbours — a rope-tension imbalance, not a rotor term. Rod's "blades initiated stationary" hypothesis is **refuted**: all 9 rings are at omega = 12.98347 rad/s, uniform and positive, at handoff | The settle places the machine and relaxes it; it does not solve the coupled position+twist equilibrium, so some node carries an unbalanced axial force. This is the defect the static solver (dynamic relaxation) exists to fix. `axial_residuals` is near zero on hub/bearing/sky, so the assembly balances along the shaft while a local node does not | **OPEN.** Diagnostic `scratch/diag_kick_decay.jl`. Remains the single `@test_broken` in `test_settle_validity.jl`, with the correct-step magnitude recorded in the test |
| 2026-09-16 | **The artificial rope damper is not needed for numerical stability, and the proposed progressive ramp-down does not help** | Constant `lin_damp` sweep (correct dt, settle 50 000 steps), handoff max abs acc: **0.05 -> 40.1**, 0.3 -> 21.0, **0.6 -> 18.5 (best)**, 0.9 -> 19.2, 0.99 -> 22.3, **0.999 -> 115.7**. Progressive ramps: 0.05->0.999 -> 27.7, 0.05->0.9 -> 18.4, 0.3->0.999 -> 30.9 — none beats a constant moderate value | The case for `lin_damp` as a stability crutch rested on the 12 123 m/s^2 blow-up, which is the dt fault above. With the correct step the settle is stable even nearly undamped (0.999 stays finite). Damping still improves handoff QUALITY, which is Rod's original start-up reasoning, but it is not a stability requirement | **No code change.** `scratch/diag_damping_ramp.jl`. Note: moderate constant damping (0.3-0.99) roughly **halves** the handoff jerk versus the canonical 0.05 — a candidate cheap win, to be weighed against the standing rule that every damper must be justified. The `lin_damp` origin row of 2026-09-12 remains OPEN |
| 2026-09-12 | **Bearing transverse damper lost in the canonical-loop refactor. The canonical loop has NO bearing damper** | The lift bearing starts 1.7 mm off the shaft axis. It is 0.48 m off-axis with 9.8 m/s transverse speed within 0.5 s of the canonical loop. It settles into a slow undamped ~0.3 to 0.5 m wobble that never decays (`scratch/bearing_motion_probe.jl`). Rod remembers the bearing "whipping about unreasonably" and needing damping | `e83f6ca` (2026-05-18) added `bearing_tr_damp = 0.99994` deliberately. "Without transverse dissipation the bearing can precess indefinitely under asymmetric bridle loading". But it added the damper to `simulate()` in `initialization.jl`. Then `run_canonical_sim!` arrived later in `12bc91b`. The refactor never ported the damper: `git log -S "_bdretain" -- src/simulation.jl` is empty. So the canonical loop (used by the evaluator + campaign) damps rope nodes only. The near-obsolete `simulate()` (4 scripts + 1 test) has the bearing damper | **OPEN.** Decision needed, not just a port. The 0.99994 figure is an empirical stand-in for drag on the fitting hardware, Dyneema viscoelasticity and rotor gyroscopic stabilisation. Rod was never happy with it as unphysical. Options: port it, derive a physical value from the dissipations it stands for, or accept the wobble and gate it. See also the wobble-policy note in handover 2026-09-11 §6.2 |
| 2026-09-12 | **No test ever asserted lift-chain TENSION. And the one that came closest runs on the wrong machine** | The only live test that asserts bridle tension is `test_bearing_alignment.jl:237` (`lowest_bridle.tension > 100.0`). It passes (216/216) on `params_10kw` with 1031 to 3492 N. But that is the **legacy 10 kW DRR machine**, not the v13 seed (`params_daisy`, 5 kW/18.8 m/6 lines). It also only reaches those numbers by dropping the hub 5 cm AFTER settling. It never asks whether the chain is taut *at the settle*. `git log -S "bridle_tension" -- test/` and `-S "cyan_tension"` return nothing, ever. The invariant `T > 0` for the lift chain has never existed as a test | Three compounding causes. First, the invariant was never written. Second, 20 of ~50 live tests still use `params_10kw`. Only 3 files carry the `ERA PIN — NOT a v13 health signal` header. So 17 assert physics on a machine the pipeline never builds **without warning**. Third, the parity test that would have flagged settle drift is gone | Guard landed: `test/test_settle_validity.jl` (V1/V2/V3/V6, 2 pass + 5 `@test_broken`), wired into `runtests.jl`. `574dfaf`. Baseline: `scratch/settle_validity_baseline.jl`. **Still open:** era-pin the remaining `params_10kw` files, or migrate them to the daisy chain |
| 2026-09-12 | **Deleted parity test blamed for a regression it could not have found. And would not have** | `test/verify_initialization_consistency.jl` (deleted `cce274e`, 2026-07-14 "Week 1 cleanup") rebuilt the hand-rolled settle in the dashboard. It required `settle_to_operational_state` to match to 1e-15. Re-run against current code it FAILS with max diff **12.85** | The premise is **obsolete, not a regression**. The differing entries are rope nodes 224 to 240 and the sky anchor. They are exactly the nodes the operational loop of the settle moves. The dashboard **no longer contains a hand-rolled settle at all**. `interactive_dashboard.jl:537` calls `settle_to_operational_state` itself. So the test compared the library against a rebuilt method that no longer exists anywhere. And "the dashboard works" can never be evidence that the settle is valid, because they are one code path | Diagnostic kept: `scratch/parity_init_check.jl` (prints the block-wise diff. Useful for localising settle drift). **Do not restore the test as a CI signal.** The real lesson: dashboard/library parity is not a validity check |
| 2026-09-12 | **Lift chain disconnected. Bridles slack. Lift never reaches the rotor** | The 6 bridles on the 5 kW/18.8 m seed read **0.0000 N** at the settle. They link the lift bearing to the main rotor. The same holds at **all 25 samples** of a 6 s run. Bearing to main-rotor distance oscillates 3.99 to 5.96 m against a 6.462198 m rest length. The model applies the lift and it never arrives. Sweeping lift-line elevation 40 to 90° does not restore it | `bearing_offset = 6.0` set the bridle rest length, a **placeholder carried over from other tested systems** (Rod 2026-09-12). The axial design point of the bearing was never chosen. The correct geometry is ~3.99 m axial / ~4.66 m 3D (apex ~31° from axis, ~59° at the ring plane). Also: before `ec5e805` the preload restore replaced RING positions only and left the bearing/sky where the equilibrium settle put them. It now recomputes rings from `sd_r` while the lift chain re-equilibrates underneath | **FIXED in the working tree 2026-09-13 (uncommitted).** A **LENGTH bookkeeping error, not a force error** disconnected the chain. The bridle rest length was cut for a 6.0 m bearing offset (`√(6.0² + 2.4²) = 6.4622 m`). The equilibrium of the chain itself puts the bearing at **3.99 m**, where the real gap is `√(3.99² + 2.4²) = 4.658 m`. The bridles were **1.80 m (39 %) too long and could never reach**. Second defect in the same place: the builder set the rest length EQUAL to the design gap, i.e. zero strain. Re-aiming the lift line or hauling the sky hook down could never have fixed this. Bridle total tension now reads **525.9 N** (was 0.000 N) and the bearing axial residual **−4.33 N** (was +226.92 N). Both now Unexpected Passes in `test_settle_validity.jl`. Still open in that guard: hub residual **+236.5 N**, sky **−81.7 N**, and first-frame acceleration **12510 m/s² (1275 g, worse than the 1051 g baseline)**. Probes: `scratch/diag_lift_chain.jl`, `scratch/design_chain_preload.jl`. See `handovers/handover-2026-09-13-lift-chain-and-realisability.md` §3 |
| 2026-09-12 | **Hub axial budget counted only the thrust of the main rotor. Wrong by 6.6×** | A force budget using the main rotor alone failed to close by ~940 N and produced a spurious "no feasible preload exists" conclusion (and a consequently wrong ring-sizing recommendation) | The seed carries **3 rotors**. Main rotor +309.4 N axial thrust, but the expansion rotors on rings 8 and 7 add **+914.5 N and +820.5 N**. **2044.3 N total**, of which the main rotor is only ~15 %. Any hub/preload budget must sum every rotor | No code change. Diagnostic corrected. `scratch/preload_thrust_budget.jl`. **Lesson recorded in `physics-topology.md` §5 item 5** |
| 2026-09-12 | **Spec update inverted the sign. The fixed point diverged** | Plan §2.4 and handover 2026-09-11 §4 both printed `T_top -= 0.7·m_hub·a_ax` with the rationale "hub accelerating up-shaft ⇒ rope pulling too hard ⇒ reduce". The converging branch of the probe was `update_sign=+1` | The rope hangs **below** the hub and pulls it **down**-shaft, so a hub accelerating up-shaft means that pull is too SMALL and `T_top` must RISE. The printed line faithfully implemented the inverted rule. Measured `df/dT = −1.073`. Corrected iteration converges in 8 steps to 1155.09 N with contraction 0.1949. The committed probe had stopped at 4 hard-coded iterations, 1158.97 N, residual −4.47 N, **not converged** | Docs corrected `dc8daff`. Lesson: verify a printed sign against measurement, and never treat a truncated iteration as converged |
| 2026-09-12 | **`capture_extended().segment_twist_deg` WRAPS the value. Hides twist saturation** | First-segment twist reported as "51.94°" (the number recorded to verify the settle fix) while the stored α gives exactly **90.00°**, the `asin` clamp ceiling | `sim_frame.jl:414` wraps Δα into (−180°, 180°] (`mod(α[i+1]−α[i]+π, 2π)−π`). A segment wound past 90° reads as a small angle, so a saturated/clamped twist looks plausible | Use the stored α block (`u[6N+1 : 6N+Nr]`) when twist is large. **Lesson recorded in `physics-topology.md` §7** |
| 2026-09-12 | **`asin(clamp(·,−1,1))` silently returns 90°** | The closed form `sin(Δα) = τ·chord/(n_lines·T_s·r_a·r_b)` cannot exceed 1. When the requested torque is unreachable at the available tension, the function returns **90° with no error** | The realisability constraint is a **per-segment tension floor** `T_s ≥ τ·chord/(n_lines·r_a·r_b)` (1227.6 N … 1274.5 N of top tension on the seed, binding segment 4). The solver truncates a violation rather than raising it. Structurally the same defect class as the `τ_carry ≤ 0` no-op | **FIXED 2026-09-13**. `trpt_matched_place` (`src/initialization.jl`) now **RAISES**, naming the segment, the demanded sin Δα and the tension ceiling. `raise_on_unrealisable=false` is a diagnostic-only escape used by the guard to measure the demand. Guards: `test/test_trpt_realisability.jl` (21 assertions) + a corrected, tightened reference in `test_settle_preload_consistency.jl`. Measured before the fix: the two non-seed cases landed as **4 × 90° (a full turn)** and **12 × 90° (three turns)** wind-ups and every guard still passed. This also RETIRES the "known tilt limitation" testset. That case was over-cliff AND its ~35 % error was the reference artefact in the row below. The new guard reproduces the independently cross-checked table in the handover (binding segment 4, sin Δα = 0.983, Δα = 79.4°, τ = 329.9 / 238.2 N·m at segs 7 / 8) |
| 2026-09-13 | **Hardcoded foreign ω in the preload reference** | `design_axial_preload` ran with its default `omega_eq`. It returned the preload for **ω = 12.983466 on every design**. So the settle guard compared a state settled at ω = 13.3995 or 11.3988 against a preload computed at the ω of the campaign seed. Reporting 2.3 % / 13.2 % "preload error" that was 100 % the wrong reference (measured **0.0** against the ω of the settle itself). It also made **ω = 0 unrepresentable** | `src/initialization.jl:961` read `ω = omega_eq > 0.0 ? omega_eq : 12.983466`. A magic constant that is the equilibrium ω of the seed itself, and the guard called the function without `omega_eq` | **FIXED 2026-09-13**. `lift_chain_design` uses the ω of the caller. `ct_at_tsr(0.0) == 0.0` makes ω = 0 an honest non-rotating state. Guard: `test/test_trpt_realisability.jl` §D, and `test_settle_preload_consistency.jl` now settles first and evaluates its reference at the ω of the settle itself (threshold 0.15 → **1e-3**). Probes: `scratch/diag_preload_reference.jl`, `scratch/diag_preload_margins.jl` |
| 2026-09-13 | **`script -q -c "<cmd>" /dev/null` masks the exit code of the child** | The command `CLAUDE.md` recommends for live suite output returns **0 even when the suite fails**, so a red suite reads as green by exit code. Verified in isolation: `script -q -c "exit 3" /dev/null; echo $?` → **0**, while `bash -c 'exit 3'` → **3** | `script(1)` on this host does not propagate the exit status of the child | **WORKAROUND**. Read the `Test Summary` line of the log / `ERROR: LoadError: Some tests did not pass` line, not `$?`. **`CLAUDE.md` still recommends the masking form and is not yet corrected** |
| 2026-09-12 | **`_matched_place_twist` silently returns the untwisted placement when `τ_carry ≤ 0`** | Every `df/dT` measurement read exactly `0.000000`. A structurally dead preload knob, so no solver on that knob could converge and two "did not converge" errors followed | `initialization.jl:972` guards the whole twist solve behind `if τ_carry > 0.0` and, when it fails, returns `Δα = 0`, `L_ax = chord0`. The *untwisted* placement, in which the geometry is independent of `F_ax`. Triggered by settling with `settle_to_equilibrium` (τ_eq = 0) instead of `settle_to_operational_state` | Diagnostics `fe2c5e3`. **Lesson: a guard that fails must raise, not return a plausible default.** `physics-topology.md` §6 |
| 2026-08-26 | **Rotor→system-ring `+1` off-by-one. Multi-rotor designs one rotor short** | A 3-rotor genome (`rotor_count_mode`) decodes `RotorSpecV10.ring_idx = [7,6,5]` but builds `ExpansionRotorParams.ring_idx = [7,6]`: the middle rotor lands on the hub (skipped by `ring_forces.jl`) and the bottom rotor lands on the middle ring. Surfaced by the new `scripts/preview_genome_geometry.jl` visual check | `expansion_params_from_rotors` (and its duplicate loop in `builders_util.jl`) used `sys_ring = rotor.ring_idx + 1`. A flown-ring-numbering leftover. `design_from_vector_v10` already emits system numbering (`ring_idx = n_rings − p + 1`, `n_rings` = total rings, hub = n_rings), so the `+1` double-counted the ground ring and shifted every expansion rotor one ring toward the hub | `sys_ring = rotor.ring_idx` (identity). Docstring states the system-numbering contract. Regression: `test/test_builders_v10.jl` unit + end-to-end. Single-rotor unaffected (FR4 holds) |
| 2026-08-24 | **~~Settle-vs-ODE 94× twist mismatch~~ → RETRACTED 2026-08-25: it was `J·θ`, not a rope-torque gap** | 2-rotor seed settles then reverses to ω<0. A first pass reported the ODE rope torque as 2.36 N·m vs the 221 N·m of the bisection ("94×") | The "2.36 N·m" figure came from `torques[1]` after the generator reaction subtraction (a net residual, ~1% convergence error). The chord model and ODE rope chain actually agree to 5 figures. The real cause is `ring_forces.jl:314`: `torques[ri] -= J_rotor·alpha[ri]` used the twist ANGLE θ as angular acceleration. A spurious torsional spring (J·θ) that braked the machine to reversal. Only fires when expansion rotors exist (why 1-rotor works, 2-rotor dies) | Blade mass is now rotary INERTIA (added to ring `inertia_z` at build, `dω/dt=τ/(I_z+J)`). J·θ term removed. Also fixed: preload used 3D chord as axial gap (over-tensioned 18 to 22×), bisection negative-twist floor |
| 2026-08-24 | **Settle ω-scan used expansion-rotor OFFSETS as absolute radii** | 2-rotor seed settled at ω=11.36 (too low). The swept area of the expansion rotor under-counted 5.8× (2.76 vs 15.96 m²) and TSR 2.4× in the settle scan | `initialization.jl` ω-scan did `area = π(blade_tip²−blade_hub²)` with `blade_tip`/`blade_hub` = 0.7·span / −0.3·span OFFSETS, dropping the `r_nominal` of the ring. ODE/Betz/gate already used absolute radii. Only the settle (and ramp evaluator `π·tip²`) were wrong | `expansion_annulus_area(er, r_nom)` + ODE mean-radius TSR. Settle ω 11.36→13.88. Fast suite 1954/1954. 2-rotor stall PERSISTS (expansion τ(ω) non-monotonic → runaway reversal). Separate open item |
| 2026-08-24 | **Corrected-geometry seed NaN-poisoned settle → "stall" (3 wrong-geometry bugs, one class)** | Seed smoke-tested `P=0, FoS=Inf, ω pinned at 60 rad/s`. Traced to `settle_to_operational_state`'s ω-scan. That scan returned the 60 rad/s fallback because its parasitic-drag term was NaN (`P_aero−NaN>P_gen` never true) | Geometric taper (`ring_spacing_v4`) makes NON-UNIFORM sub-segs (the shortest sub-seg of the seed = 0.3119 m, while `dt=4e-5` assumed 0.5 m). Three consumers assumed uniform spacing/fixed dt. First, settles keyed dt off `n_lines≥8` not Lmin, so rope positions went NaN at settle step 41. Second, the axial-preload restore used uniform `seg_len=tether_length/n_seg`, so the bottom ring landed 2.15× out → 115% strain → break. Third, the scoring window `V11_DT=4e-5` destabilised the 0.31 m sub-seg from the settled state → 41% strain spike → break | `stable_dt_for_system` = `min(4e-5, 4e-5·√(Lmin/0.5)/1.5)` (Rod: 1.5× margin), threaded through both settles AND the window. Preload restore uses per-segment rest length + stiffness. Seed now runs to P=5.39 kW / 0.00% lift err, but FoS_min=1.01 (genuinely weak, needs re-seed). Fast suite green |
| 2026-08-24 | **Winner re-gate ran at a stale k_mppt (5.39, not the campaign value 2.24)** | The 5 kW campaign re-run winner was re-gated and reported 'PASSES, P_gen 6.39 kW'. But that verdict came from a run at k=5.39, a DIFFERENT machine than the campaign ran (k=2.24) | `scripts/ode_gate_v13.jl` hardcoded `k_mp = KW == 5.0 ? 5.39 : p.k_mppt` (the superseded 20 s-window sweep value).  The runner + smoke moved to k=2.24 (honest-window sweep) but the gate kept the old value. The same k-alignment fault class as the 2026-08-13 'evaluator tested at k_mppt=10.0' row.  Caught in pre-push review (Hermes) | gate k → 2.24. Winner re-gated at the campaign operating point: PASSES, P_gen 5.18 kW flat (5.09→5.18 over 30 s, ω 13.22 rad/s settled). `026c734` |
| 2026-08-22 | **Non-finite FoS passes the mass-min floor. FIXED same day** | `mass_min_fitness` (objective_v12.jl) and `v12_fitness` test `FoS_min < fos_hard` without an isfinite guard. Then `FoS_min = Inf` (null structural measurement) makes `Inf < 2.5` false. So a machine transmitting ≥5 kW with unmeasured ring loads passes the FoS floor and scores its mass. `v11_fitness` likewise scored FoS=Inf as the BEST fitness (-P) | The fix for exploit-register row 1 ("Inf FoS → feasible") landed ONLY in `objective_feasibility` (5d02d45). The mass-min objective (2026-08-20), v12_fitness and v11_fitness never got the guard. The running 5 kW campaign used the unguarded evaluator. Telemetry showed zero FoS=Inf ok-rows. The re-gate screens winners | `(!isfinite(FoS_min) || FoS_min < fos_hard) && return Inf` in mass_min_fitness + v12_fitness. `!isfinite(FoS_min) && return Inf` in v11_fitness. Test/test_fos_guard.jl (10 assertions) wired into runtests.jl. Fast suite green |
| 2026-08-14 | **Hub-ring numerical divergence passed all ground-side metrics** | 18m v13 winner (fitness −6.66) shows ω_hub=3.5e66 within 5s of the gate. The state is NaN-frozen (identical checkpoints for 30s). Meanwhile ω_gnd=16.41, P_gen=8.60 kW, twist ratio 0.0 | r_hub=0.474 (DE shrank toward the 0.2 bound). The design has an inverted taper and n_active=1. That gives tiny hub-ring inertia + freewheel + MPPT at ground → hub ring diverges. Twist α NaN-freezes (reads 0), the FoS of the diverged ring filtered as NaN. Every instrument was ground-side. The Betz gate is OUTPUT-side: generator power (8.6 kW) was below the swept-area ceiling, so it passed. It never checks intermediate ring states | `tip_speed_sanity_ok(u, sys)`: every ring rim + every rotor tip ≤ 100 m/s (Rod 2026-08-14. Design point ~44 m/s at TSR 4, 11 m/s). Hard reject in evaluate_windowed + gate. r_hub lo raised 0.2→0.7. Acceptance B6/B7 (7183f96, tightened same day) |
| 2026-08-13 | **Evaluator tested 5 kW designs at k_mppt=10.0** | 18m flywheel winner showed P_end 4.18 kW in the evaluator vs 2.26 kW at the gain of the system itself (gate). Window power inflated ~2× | `ObjectiveConfig.k_mppt` default (10.0) is the 50 kW-scale controller gain. The v12 5 kW campaigns never overrode it. ~5× the rated gain of the scaled system (p.k_mppt ≈ 1.94) | v13 cfg sets `k_mppt = p.k_mppt`. Caught by acceptance test B2 |
| 2026-08-13 | **Kickstart winds healthy chains past δα*** | Seed evaluated `:reject, twist_crossed=true` in v13 while the gate (no kick) showed ratio 0.3 | Cold-path 2 s PTO motor kick (k=−60, ~115× MPPT gain, ~21.7 kN·m at ω≈19) is a legacy ζ=1.5 stall escape. Post-ζ-fix the settle reaches the productive branch directly and the kick itself violates the collapse limit | `ObjectiveConfig.kickstart_s` (default 2.0 = v12. V13 sets 0.0). Caught by acceptance test B3 |
| 2026-08-13 | **Twist detector read the ω block, not the α block** | B1-B3 all flagged twist_crossed=true (even the healthy seed). B5 unit test showed max_ratio=0.0 on a +π-wound state | ODE state layout: α at `u[6N+1:6N+Nr]`, ω at `u[6N+Nr+1:6N+2Nr]`. The first detector draft read `u[6N+Nr+ri]` (ω block). Post-settle ω diffs ≈ 0 (ratio 0.0), window ω differentials (rad/s) compared against δα* (rad) → spurious collapse flags everywhere | Correct index: `Δα = u[6N+ri+1] − u[6N+ri]`. Test B5b (+π wound α) is the unit guard against the block confusion |
| 2026-08-13 | **Gate/evaluator read ω_hub, not ω_gnd** | 5kW designs "sustain" 6.34 kW at 20s but decay to ~2 kW by 60s. Power budget shows P_gen→0 while hub freewheels | Gate P = k·ω_hub³ but generator extracts k·ω_gnd³ (Mode 0, ground ring). After t≈50s the torsional chain decouples: ω_gnd→0, ω_hub stays ~9 rad/s | `scripts/ode_gate_v13.jl`. P_gen = τ_gen·ω_gnd via `get_generator_torque` + per-segment twist detector vs δα* (acceptance tests A1 to A4 green 2026-08-13. Island-1 winner now FAILS: twist ratio 169×, P_gen 0.84 kW). See `docs/plans/2026-08-13-gate-reinstrument-twist.md` |
| 2026-08-13 | **Torsional collapse CONFIRMED at 5kW (top segment)** | Twist concentrates at ring10→hub: Δα grows to 22,425° (62 revs) vs 42.6° collapse limit. Brake=false | Inverted taper (r_ground 2.5m > r_hub 0.65m): τ_cap ∝ r_min² → narrow hub end is weakest segment. DE shrank r_hub for mass | Static torsional gate was RIGHT (scored 0.31). Do not relax it for ≤7kW. Re-enable as hard gate |
| 2026-08-12 | **ζ=1.5 rope damping + tension rectifier = DC reverse torque** | EVERY ODE test stalls to ω≈−0.2 rad/s regardless of genome/k_mppt/startup. BEM predicts 22 kW, ODE produces ~0 | `initialization.jl:97` hardcoded zeta=1.5 (~30-150× physical Dyneema). `max(0.0, EA·ε+c_damp·v)` rectifier in `rope_forces.jl:19` clips the damper asymmetrically → non-zero mean force (DC bias) that overwhelms aero torque | ζ promoted to `SystemParams.zeta` default 0.05. `initialization.jl` uses `p.zeta`. See DECISIONS.md [2026-08-12] + `handovers/handover-2026-08-12-zeta-damping-fix.md` |
| 2026-08-12 | ~~Static evaluator false-negative at 5 kW~~ **SUPERSEDED 2026-08-13** | `evaluate_design_v5` rejects ALL 5kW designs: torsional FoS < 1.5. Ramp evaluator `solve_equilibrium_self_consistent` returns nothing | ~~τ_cap ∝ T_total ∝ P. 50kW-calibrated gates unreachable at small scale~~. The ODE later CONFIRMED the torsional gate: the chain of the 5kW winner collapses at the top segment (Δα=22,425°). The gate was right. The ODE gate read the wrong ring (ω_hub not ω_gnd). Daisy tors=0.22 remains an open question (its geometry differs. 13 dense lower rings per Tulloch) | Re-enable torsional gate for ≤7kW. See fault row above |
| 2026-08-12 | settle_to_operational_state ω_rated_max clamp masks genome variation | 57-variant perturbation sweep: Δω=0.000 across ALL 14 dims | settle clamps ω at the `ω_rated_max` argument. Passing 9.5 clamps every genome to the ceiling. True equilibrium ≈16 rad/s | Pass ω_rated_max=60 (or sky-high) when measuring equilibrium ω per-genome |
| 2026-07-22 | FoS sign inversion | DE rewarded structural failure (fitness 50× better at FoS 0.03) | `fitness = -P * fos_penalty` with `fos_penalty > 1` when failing | `9bd3f67`: `*` → `/`. Extracted `v11_fitness()` with monotonicity unit tests |
| 2026-07-22 | Missing orbital-velocity init | Uniform FoS=0.03-0.08 band. 260 kW above Betz ceiling. P_range 218-1793 kW | Warm-start set ring ω=ω_eq but node velocities stayed at zero → kinematically impossible state | `9bd3f67`: added tangential `v=ω_eq×r` block per `recheck_12gon_convergence.jl:74-84` |
| 2026-07-22 | Wrong protocol in anchor batch | k_best → 1000 bound. LHS 0/40 valid. Overnight batch produced garbage | `recampaign_anchors.jl:100` called `warmstart_with_k_bracket` (flawed fast path) instead of full-protocol fallback | Superseded anchors.csv with banner. Batch not yet re-run |
| 2026-07-22 | Forward Euler blowup on k·ω² MPPT | 1000-1900 kW super-Betz spikes. FoS floor 0.04-0.13 across all k | `simulation.jl:159`. Explicit Euler on positive-feedback `τ=k·ω²` term, no magnitude cap | `d285139`: semi-implicit ground-ring update `ω' = (ω+dt·τ_other/I)/(1+dt·k·ω/I)` |
| 2026-07-22 | dt-unscaled orbital damping | DT-refinement gets WORSE with smaller dt (DT/4 → 10⁷⁶ kW overflow) | `initialization.jl:539`. `v_orbital + lin_damp * v_osc` applied per-step without dt scaling. Halving dt doubles projections | FIXED: all three per-step velocity dampers dt-scaled via `exp(-rate·dt)`. `orbital_damp_rope_velocities!` (`f71c7a0`), `settle_to_equilibrium` velocity kill, `simulate()` bearing damper (`17cc6f6`). Semi-implicit k·ω² braking also applied (`d285139`). Integrator loop is clean. Residual DT-refinement non-convergence at lin_damp=0.05 comes from damping-rate calibration (see Unanchored Parameters). |

---

## Cross-Geometry Spot-Check: v13 Winner (2026-09-21)

Instrument validation result and advisory finding. Does not block the wobble-gate exit decision (wobble-gate scope ruling).

**The instrument generalises.** The full wobble protocol ran on the last v13 winner. The machine differs from the seed: single rotor, 3 lines, 6 rings, r_hub 4.32 m. No reader or plumbing fault appeared. Every instrument (FoS, tensions, power, omega) reads coherently. Probes: `scratch/probe_wobble_gate_run_winner.jl`, `scratch/probe_wobble_gate_run_winner_nop30k.jl`, analysis `scratch/analyze_winner_spotcheck.py` (HEAD `9e40342`, re-runs `dcc1f26`, docs-only delta).

**The winner does not settle.** The hub walks out during the relax phase (0.34 m at t = 20 s, 1.8 m by t = 100 s, deep run). The window then shows a sustained violent limit cycle. FoS trough 0.28 against the 2.5 gate (0.15 with the shallow settle). The hub swings 3.7 m peak-to-peak about a 2.7 m offset. Top-bay tension spikes reach 9.0 to 11.8 kN. All 15 TRPT lines flick slack (worst contiguous dip 3.8 s). The bridle cone carries almost nothing (mean 1.3 N deep, 0.0 N shallow). No rope break and no non-finite value.

**Attribution checks.** Not the 2026-09-20 physics era: the canonical re-gate still PASSES at HEAD (P_gen 5.674 kW, omega_gnd 13.63, crossing ratio 0.52). Not the settle depth: the n_op = 30 000 twin shows the same violent window.

**Gate blind spot.** The canonical power gate has no FoS metric and a 30 s window. Its PASS never covered long-horizon stability. Any reuse of this winner needs re-qualification under the current model.

**Damping follow-up (2026-09-21).** A re-run at lin_damp = 0.60 softens the load violence: FoS trough 0.68, top-bay max 2.3 kN, worst TRPT dip 1.7 s. The machine still self-excites though. The hub swings 2.8 m peak-to-peak and FoS stays below 2.5 on 57 of 216 samples. Every line still flicks slack. Artificial damping tames the spikes, not the orbit. Both damping levels fail the honest gate on the FoS trough.

**Re-run under the landed lifter boundary (2026-09-21, `1c9f9cb`).** The crosswind symmetry fix and the along-line damping are both active in the run. Top-bay load spikes halve (9.0 to 4.6 kN) and the FoS trough improves (0.28 to 0.40). The top bay holds stable for about 80 s. Beyond 90 s the machine still enters its 3.7 m orbital limit cycle. That motion is intrinsic rotor and shaft windup of this genome, not a lifter boundary fault (`DECISIONS.md` [2026-09-21], `329c8d2`). The desktop full protocol and the laptop 120 s chunk probe (`scratch/verify_canonical_winner_120s.jl`) agree on the trajectory. The winner still fails the honest gate. Its long-horizon behaviour is recorded here so no instance re-derives it.

---

## Unanchored Parameters

Modeling choices that affect results but have no physical calibration. Ranked by sensitivity. Top items need hardware anchors before any external claim.

| Parameter | Current Value | Physical Meaning | Sensitivity |
|-----------|--------------|-------------------|-------------|
| `lin_damp` (rope damping rate) | 0.05 → rate=75,000 Hz | Per-step orbital velocity retention. At production DT damps rope oscillations in ~13 µs. Near-rigid orbital slaving. Never physically calibrated. | **HIGH**. Orbital damping ΔL_z measurement (2026-08-12) shows near-zero cumulative bias, exonerating it as the reverse-torque source. The "alive or dead" behaviour previously attributed to this rate was actually ζ=1.5 (see Fault Ledger 2026-08-12). Still unanchored, but no longer suspected of causing the stall. |
| α-constants (Tulloch et al.) | Various | Induced-drag correction factors for expansion rotors | Known. See `DECISIONS.md` |
| `TRPT_REALISABILITY_TENSION_MARGIN` | 1.05 (`src/initialization.jl`) | Tension the design preload must hold **above** the per-segment torsional realisability floor `T_s ≥ τ·chord/(n_lines·r_a·r_b)`, i.e. `sin Δα ≤ 1/1.05` | **HIGH**. Design policy, not a measurement. Chosen 2026-09-13 to clear the measured worst shortfall of the seed family (~3.7 %. Several seeds were past the cliff and only passed because `asin(clamp(·))` placed multi-turn wind-ups). Raising it raises transmission tension → ring compression → FoS demand → sizing, so it needs the load → `SIZING_FOS_MARGIN` → acceptance re-baseline of handover 2026-09-13 §11 step 6 |
| `TRPT_REALISABILITY_MAX_PRELOAD_FACTOR` | 1.5 (`src/initialization.jl`) | Largest factor `design_axial_preload` may raise the top tension to clear the floor before it REFUSES the design | **MEDIUM**. An engineering bound, not a physical quantity. Above it the solver raises (`physics-topology.md` §6) instead of silently rescuing a genome tens of times past the cliff (e.g. the pre-fix 18 m winner at sin Δα = 35.6, which would need a ~37× preload) |

The `lin_damp=0` (no rope stabilization) case produces trivially-convergent but unphysical results. It is NOT a design finding and must not count as proof that the structure is inadequate ("FoS=0.18 → rings buckle"). The canonical config is `lin_damp=0.05`, spokes ON.

---

## Detection Pattern: Instrument Floor

**Rule:** A metric that is uniform across conditions that SHOULD change it is an instrument floor, not a design finding.

Three validated examples (this week):
1. **Warm-start FoS:** 0.028-0.075 for designs spanning 39-260 kW → initialization shock, not structure
2. **k-bracket selecting failure:** k_best→1000 bound because sign inversion made failing designs score better
3. **FoS-vs-k sweep:** FoS 0.04-0.13 across k=20-80 despite power varying 1.3-17.7 kW → integrator blowup, not ring strength

**Also: P > Betz ceiling → broken state, not data point.** 1000+ kW from a ~97 kW machine is physically impossible.

---

## Detection Pattern: Peak-Value Metrics Alias on Limit Cycles

A convergence or comparison gate built on `P_max` (or any single-sample peak) is
phase-sensitive: DT and DT/2 runs decorrelate in phase and hit different peaks of
the SAME physical limit cycle, so their peak ratio differs even when the integrator
is fully converged. **Symptom:** an isolated "valid" point flanked by "divergent" ones,
with the divergent points showing SMALL peaks (not exploding).

2026-07-22: this misclassified `lin_damp=0.5` as divergent in the damping-rate
sweep. The real valid band was 0.5 to 0.8. **Rule:** gate convergence on windowed
statistics (mean + range agreement), never on peaks. Genuine divergence *explodes*
the peak (thousands of kW). Phase-noise jitters it by 1.5 to 2×.

Before launching any campaign, confirm that all convergence/promotion gates use
windowed agreement, not a peak or endpoint. A peak-based gate anywhere in the
objective pipeline would quietly reintroduce this error.

---

## Sanity Bounds (Hard Discard Rules)

The pipeline discards any result that triggers these, without analysis:

| Check | Threshold | Action |
|-------|-----------|--------|
| P_aero > Σ-annulus Betz ceiling | ~97 kW for 12-gon, 5m rotor | Discard sample. Integrator instability |
| ω·r_tip > sane tip speed | ~200 m/s (~380 rpm at 5m) | Discard sample. Numerical blowup |
| FoS identical (±20%) across >3 k values | n/a | Instrument floor. Do not draw structural conclusions |
| P_range / P_mean > 10 | n/a | Limit-cycling. Not a steady-state measurement |
| dt-refinement: DT vs DT/2 vs DT/4 diverge | n/a | Integrator not converged. Results at any single dt are not trustworthy |

---

## DT-Refinement Outcomes

The three possible results of a DT/DT2/DT4 comparison:

| Outcome | Spikes/FoS as dt→0 | Meaning | Action |
|---------|---------------------|---------|--------|
| Convergent | Vanish toward zero | 100% numerical. Integrator issue | Fix integrator. Result at one dt is the wrong number |
| Persistent | Unchanged within 10% | Genuine dynamics (stiff physical process) | Physics question, not numerical |
| Divergent | Get WORSE with smaller dt | dt-unscaled operator (e.g., per-step projection) | Fix dt-scaling on the operator. Result at any dt is coincidental |

Our k=60 case: **Divergent**. 2→57→88 super-Betz samples, P_max 38→2798→∞ kW. This is the textbook signature of a dt-unscaled per-step operator, not a CFL limit and not a stiff physical process.

---

## Fix Hierarchy

1. **dt-scale per-step projections first.** These are the first-order culprits. They produce divergence-under-refinement that masks everything else.
2. **Semi-implicit on positive-feedback terms.** Second-order, and it applies after dt-scaling of the projections. Prefer over global dt reduction (4× slower) and reject-and-flag (hides instability instead of removing it).
3. **Then DT-refinement.** After both fixes, DT ≈ DT/2 ≈ DT/4 must hold. Only then does a single-dt result become trustworthy.

---

## Both Over-Corrections Pitfall

| Wrong | Why |
|-------|-----|
| "Structurally dead at any k" | Reads the blowup as design property. K=40 already falsified this (0/301 FoS dips) |
| "Fine, just numerics" | Assumes the fix reveals power that may not be there. Low-k stable but low-power (~1.6 kW) |

The truth sits between: the instrument was broken, the design has no measurement, the real operating envelope is unknown until the integrator converges under refinement.

---

## Pre-Flight Checklist (Before Any Batch Launch)

- [ ] `git pull`. Sync with other instance
- [ ] `julia --project=. test/test_objective_v11.jl`. Pure unit tests pass
- [ ] `v11_fitness()` monotonicity test passes (worse FoS → worse fitness)
- [ ] Warm-start uses BOTH ω AND tangential v init
- [ ] Full protocol uses `settle_to_operational_state` (not plain settle)
- [ ] DT-refinement pass at target k: DT≈DT/2≈DT/4 within 15%
- [ ] No super-Betz P_aero samples at production DT
- [ ] FoS varies with k (not uniform). Confirms the instrument measures structure, not floor
- [ ] `lin_damp` is dt-scaled ✓ (confirmed post-fix)
- [ ] `python3 scripts/kwarg_default_audit.py --check`. No undocumented HIGH-danger defaults
- [ ] Provenance stamp on output CSV: git hash, physics-era, geometry fingerprint, instrument version

---

## Superseded Artifacts

| File | Status | Reason |
|------|--------|--------|
| `anchors_SUPERSEDED_2026-07-21.csv` | Banner, retained as evidence | Inverted fitness, shocked states, k-bracket selecting for failure |
| `k_sweep_full_20260721_1612.csv` | Banner, retained as evidence | Pre-semi-implicit-fix. FoS floor is integrator blowup |
| `kickstart_sweep_12gon.csv` (2026-07-17) | Reference only | Different protocol (30 rad/s kickstart), not comparable |

---

## Canonical Config

Settings that must match between instances for results to be comparable:

```
DT = adaptive      (was fixed 4e-5 = V11_DT; now `stable_dt_for_system` = min(4e-5, 4e-5·√(Lmin/0.5)/1.5) — the geometric taper shortens sub-segs below the 0.5 m that 4e-5 was calibrated for. 2026-08-24)
wind = 11.0 m/s    (rated wind speed)
power_W = 50000    (rated power)
v_rated = 11.0     (rated wind)
elev_angle = π/6   (30°)
spokes = ON        (SpokeParams(enabled=true) — radial ties are real structural elements per 2026-07-06 design change; OFF contradicts 32 production scripts and would let different instances configure different physical systems) [CORRECTED 2026-07-22: was OFF]
FOS_DESIGN = 1.5   (minimum acceptable FoS)
lin_damp = 0.05    (dt-scaled post-fix — confirmed convergent under DT-refinement)
```

---

## Queued Gates

Gates that must pass before any external claim about "what this design can produce":

| Gate | Status | Description |
|------|--------|-------------|
| Damping-rate sensitivity sweep (DT-paired) | ⏳ QUEUED | Sweep `lin_damp` across candidate rates. For EACH rate, run DT AND DT/2. A rate is valid only if alive (non-trivial P) AND dt-convergent (DT≈DT/2 within 15% on P_max and FoS_min). Single-dt results without refinement are not trustworthy. If no rate at which the 12-gon is both alive and dt-convergent, that is a design finding (just not the one we thought). If a valid rate exists, it joins the α-constants as an unanchored parameter needing hardware calibration. |




