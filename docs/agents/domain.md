# Domain Documentation

This repository uses a single-context layout for domain documentation.

## Locations

- **Context**: `CONTEXT.md` at the repository root.
- **ADRs**: `docs/adr/` directory.

## Consumer Rules

Skills like `improve-codebase-architecture`, `diagnose`, and `tdd` will look for these files to understand the project's domain and architectural history.
