# KTD.jl test-suite inventory — LIVE vs LEGACY verdicts vs the v13 pipeline

Repo: /home/rod/Documents/GitHub/KiteTurbineDynamics.jl @ HEAD 0b77b53 (`feat: aligned-FoS structural layer + orientation fixes + solve_ring_Do`), clean tree.
Method: read both runners' include lists; read every test file; traced every call with `grep` across src/ and scripts/; read the v13 entry scripts in full (run_v13_5kw_masslift.jl, ode_gate_v13.jl, compute_seeds.jl, smoke_masslift_v13.jl, regate_winner_v13.jl, combine_islands_v13.jl) and the evaluator/builders they reach.
"LIVE" = reachable (transitively) from the current v13 mass-aware const-tension campaign chain (runner → evaluate_windowed / gate scripts → decode → build → ODE). Acceptance suite NOT run. Two targeted runs confirmed green: `test_fitness_appropriateness_2026_09.jl` (13/13 pass, 7 s) and `test_builders_v10.jl` (all pass, 8.4 s); no julia process was running beforehand.

A note on geography (differs from the brief): the V10 decode is `design_from_vector_v10` in **src/objective_v10.jl:129** (not trpt_optimization.jl); the ODE system builder is `build_system_from_v10` in **src/objective_evaluator.jl:322**; trpt_optimization.jl holds the **mass model** (ring_beam_mass :88, knuckle_mass_at_ring :59, tube_wall_thickness :75, MIN_TUBE_WALL_M :32, OPT_* consts :25-40) plus the **v1 TRPTDesign static-optimiser stack** (:193-941).

## 1. Per-file table (48 files; registration = runtests.jl include line / acceptance_runtests list)

Verdict scale: **LIVE** (all asserted code reachable from v13 chain) · **PARTIAL** (mix) · **LEGACY-ONLY** (nothing it asserts is on the v13 chain). src defs cited as `srcfile:line`; test lines as `L#`.

| # | File | Header purpose (top comment) | @testsets | Key src/scripts code exercised (test-file evidence) | Reg. | Verdict + one-line reason |
|---|---|---|---|---|---|---|
| 1 | test_aerodynamics.jl | Cp/Ct table sanity (no header; inline note) | aerodynamics | cp_at_tsr, ct_at_tsr (L4-10); defs aerodynamics.jl:263+ | runtests:17 | **LIVE** — table feeds the ODE settle scan (initialization.jl:942) and ring torque on the v13 path |
| 2 | test_airborne_fos.jl | 2026-08-25 off-by-one: ring_fos[1] is the lowest floating ring | min_airborne_fos — includes index 1 | min_airborne_fos (L17-43); def objective_evaluator.jl:44, used in evaluate_windowed L733 | runtests:48 | **LIVE** — pins the FoS-min authority evaluate_windowed uses every window sample |
| 3 | test_bearing_alignment.jl | Frame-0 lift-bearing alignment + bridle equality after settle; IMU damping; brake Euler constraint | bearing alignment at frame 0; gold bridle tension; Field IMU active damping; brake Euler velocity constraint | params_10kw(), build_kite_turbine_system, settle_to_operational_state, rotary_lifter_default, apply_brake_constraint! (L31-32,43,428); update_ramp/RampController (L240-384) | runtests:33 | **PARTIAL** — asserts on a params_10kw legacy-built machine, but the exercised kernels (settle_to_operational_state def initialization.jl:882, apply_brake_constraint!, ramps) are the same code the v13 cold path runs |
| 4 | test_bem_unified.jl | Unified cp_bem/ct_bem vs AeroDyn baseline | Match AeroDyn at n_lines=5; Cp monotonic; CT scaling; Rotor radius self-consistent; Physical bounds; TSR dependence | BEM.cp_bem/ct_bem/rotor_radius_for_power (L19-88) | runtests:18 | **LIVE** — rotor_radius_for_power sizes every v13 rotor (objective_v10.jl:237,267) |
| 5 | test_blade_geometry.jl | ring-anchored 70/30 blade split (tip +0.7·span, hub −0.3·span) | V10 Tight λ=1.0; λ=0.69; V10 Reinforced; ExpansionStackConfig defaults | build_v10_tight_no_lowest via builders_util (L17-19,33,45); ExpansionStackConfig (L59) | runtests:19 | **LEGACY-ONLY** — only exercises build_v10_tight* (no v13 callers) and build_expansion_stack (only caller objective_v6.jl, legacy); the 70/30 invariant is re-pinned on the live decode elsewhere (§3) |
| 6 | test_blade_mass_law.jl | unified m_blade = m_ref·span³ (0.420 kg anchor), knuckle floor, fingerprint | reference anchor; expansion blade mass span³; build_system_from_v10 prices decoded span; knuckle floor + airborne accounting; geometry_fingerprint no double-count; linear taper no negative radii | M_BLADE_REF_KG (L19), params_daisy (L21), expansion_blade_mass (L27-39; def expansion_rotor.jl:488, live caller builders_util.jl:106), design_from_vector_v10 + build_system_from_v10 + expansion_airborne_mass (L48-104), compute_seeds.jl include, geometry_fingerprint (L120) | runtests:20 | **LIVE** — pins the exact mass law the v13 evaluator implements (objective_evaluator.jl:343-416) and the decode=builder radius identity (L154-155) |
| 7 | test_builders_v10.jl | corrected V10 builder gates 1a-1c, rotor-mask mapping, decode→build ring placement | V10 builder 13-gon geometry; round-trip guard; r_bottom_scale single-application; blade_scale leaves rings untouched; Gate 1b drop; Gate 1c n_blades=n_lines; expansion_params_from_rotors ×3; decode→build placement (2026-08-26) | build_v10_tight_no_lowest/tight (L11,33,43...), design_from_vector_v10+params_v5_50kw (L30), expansion_params_from_rotors (L99-131; def builders_util.jl:79), params_daisy/mass_scale/override_params (L141-148), live decode+build (L150-163) | runtests:43 | **PARTIAL** — first 7 testsets pin the legacy tight builder + v10_campaign_50kw artifact (no v13 callers); L83-131 (rotor→ring mapping) and L134-164 (v13 decode→build) are LIVE: expansion_params_from_rotors is called by objective_evaluator.jl:343,574 |
| 8 | test_campaign_k_alignment.jl | k single-source static guard (trust-log 08-13/08-24 drift) | campaign k single-source | source-text scan of run_v13_5kw_masslift/smoke/ode_gate/analyze_campaign_winners for K_MPPT_5KW_HONEST (L13-37) | runtests:21 | **LIVE** (meta-guard) — polices the v13 chain's own scripts; asserts constant == 2.24 (compute_seeds.jl:34) |
| 9 | test_dashboard_smoke.jl | headless dashboard data-layer smoke | dashboard build smoke | params_10kw + build_kite_turbine_system + settle_to_operational_state + run_canonical_sim! + capture_frame/peaks (L5-46) | runtests:41 | **PARTIAL** — exercises the live sim/capture kernels but only on a legacy params_10kw machine (dashboard defaults); no v13 decode coverage |
| 10 | test_documented_claims.jl | guards externally-quoted numbers | legacy triangle anchor (Strathclyde); V10 Tight builder fingerprint; V6.2 recovered optimum; params_10kw internal consistency | CSV artifact reads + params_10kw m_blade=11/5 (L17-74) | runtests:46 | **LEGACY-ONLY** — every pinned number is v10-era/DRR-era (12-gon 2.889/2.0 m, v6.2 74.17 kg, params_10kw blade mass); v13-era claims (420 g Daisy anchor) are pinned in test_blade_mass_law.jl:19-21 instead. Still a deliberate provenance guard, but it cannot fail on v13 drift |
| 11 | test_dynamics.jl | ODE smoke | ODE smoke test | params_10kw + build_kite_turbine_system + multibody_ode! (L2-9) | runtests:30 | **PARTIAL** — multibody_ode! is the live integrator target (simulation.jl), but machine = legacy 10 kW build |
| 12 | test_emergent_torsion.jl | twist restoring torque emerges from rope geometry | emergent torsion | params_10kw build + compute_rope_forces! (L4-20) | runtests:34 | **PARTIAL** — compute_rope_forces! is the live ODE rope kernel; legacy params/builder for the fixture |
| 13 | test_evaluator_v13.jl | acceptance B1-B7 (v13 evaluator instrumentation), re-baselined 2026-09-04/07 | B1 collapse reject; B2 bounded; B3 seed :ok P_end≥5; B4 unit penalize_ceiling; B5 twist_collapse_check; B6 aligned-FoS rejects winner; B7 tip_speed_sanity_ok | evaluate_windowed cold + appropriate_mass_fitness + params_at_length(params_daisy) + gate_design (L61-72, 162); includes compute_seeds.jl + ode_gate_v13.jl | acceptance list | **LIVE** — the v13 chain itself, end to end |
| 14 | test_expansion_analysis.jl | φ (mass/power) tracking + telemetry | airborne mass baseline; expansion rotors add mass; phi; effective radius summary; telemetry record; phi improvement | params_10kw + build_kite_turbine_system + build_expansion_stack + expansion_airborne_mass/phi/radius_summary/telemetry (L5-105) | runtests:25 | **PARTIAL** — expansion_airborne_mass is the live v13 mass (objective_evaluator.jl:896); expansion_phi/telemetry/radius_summary have **zero src callers** (analysis-layer, old scripts only) and the fixtures are legacy 10 kW builds |
| 15 | test_expansion_induction.jl | per-annulus induction fixed point + α model + FR4 toggle inertness | Betz cap + convergence; continuity + design point; Daisy rotor exercises model; FR4 no-expansion bit-identity | solve_expansion_induction/expansion_rotor_forces/expansion_cl/expansion_annulus_area + physics toggles (L43-116); run_canonical_sim! (L112-115) | runtests:44 | **LIVE** — induction is the live expansion-rotor aero (ring_forces.jl, initialization.jl settle), and FR4 runs the shared run_canonical_sim! |
| 16 | test_expansion_rotor.jl | analytical expansion-rotor mechanics (LEGACY physics pin, 07-18) | zero-wind spreading; zero bank; v² scaling; bank; radius; empty stack | expansion_rotor_forces under LEGACY_PHYSICS toggle (L7-104); estimate_effective_radii + params_10kw (L93-99) | runtests:23 | **PARTIAL** — expansion_rotor_forces itself is live (ring_forces.jl, sim_frame.jl:381 area, initialization.jl); the file asserts the *legacy-CL* variant of it and one legacy helper (estimate_effective_radii def objective_v6.jl:149, src callers only v6 legacy) |
| 17 | test_expansion_stack.jl | expansion-stack configuration generator | alternating; clustered; custom; zero; clamped; bank stored | build_expansion_stack only (L20-143) | runtests:24 | **LEGACY-ONLY** — build_expansion_stack's only src caller is objective_v6.jl (legacy v6 era); v13 rotors are placed by decode + expansion_params_from_rotors |
| 18 | test_fitness_appropriateness_2026_09.jl | T3 appropriate_mass_fitness relative ordering (placeholder weights) | hard reject gates; over-power (a); beam-utilisation (b); over-twist (c); zero-penalty collapse | appropriate_mass_fitness (L22-69); def objective_v12.jl:145; ObjectiveConfig 5 kW cfg (L17-18) | runtests:57 | **LIVE** — the runner's exact fitness seam (run_v13_5kw_masslift.jl:275) |
| 19 | test_fos_guard.jl | non-finite FoS cannot pass hard floor (exploit row 1) | non-finite FoS hard floor | mass_min_fitness/v12_fitness/v11_fitness guards (L22-33) | runtests:47 | **PARTIAL** — asserts legacy adapter fns (v11/v12) + mass_min_fitness; the same guard is live only through appropriate_mass_fitness (objective_v12.jl:149). A regression in appropriate_mass_fitness alone would NOT fail this file |
| 20 | test_gate_v13.jl | acceptance A1-A4 re-instrumented ODE gate | A1 winner passes; A2 twist<limit; A3 no post-settle flag; A4 P_gen = τ_gen·ω_gnd | gate_design + twist_report (ode_gate_v13.jl), lift_for, settle, get_generator_torque (L36-72) | acceptance list | **LIVE** — executes the v13 gate module it includes |
| 21 | test_geometry.jl | (no header) pure geometry helpers | geometry | shaft_perp_basis, attachment_point, rope_helix_pos (L6-39) | runtests:27 | **LIVE** — pure helpers used by ODE init/rope geometry on every build path incl. v13 |
| 22 | test_golden_traces.jl | physics-core golden traces (sin-not-tan, spacing, beam sections) | Polygon force resolution; Ring spacing v4 analytic; Beam section circular tube | pure math; ring_spacing_v4 (L29; def ring_spacing.jl:60); BeamSpec/beam_section_properties (L43-46; def trpt_optimization.jl:240) | runtests:22 | **PARTIAL** — ring_spacing_v4 analytic invariants are live physics (warm path objective_evaluator.jl:576, decode geometry); beam_section_properties is v1-static-solver-only (legacy) |
| 23 | test_lift_kite_rotary.jl | RotaryLifter force model (PCA-2) | RotaryLifter force | rotary_lifter_default, lift_force_steady (L5-22; defs lift_kite.jl:233+) | runtests:53 | **LEGACY-ONLY** (vs masslift chain) — rotary lifter = superseded fixed-rotary regime (v13_5kw era); the current chain uses sized_lifter_for/StackedLifterParams const_tension=true (lift_kite.jl:197) |
| 24 | test_lift_kite_stacked.jl | StackedLifter const_tension capability (08-18 ruling) | StackedLifter const-tension | StackedLifterParams T_ref flatness + 1.5·m·g/sin70 rule + sized_lifter_for round-trip on a v10-built 5 kW system (L31-71) | runtests:54 | **LIVE** — pins the campaign lift law (T_ref = 1.5·m_airborne·g/sin(70°), lifter self-mass excluded) |
| 25 | test_mass_model_2026_09.jl | T1 mass model: 2 mm wall floor, per-ring sum, ring→cable knuckles | 2 mm wall floor; min_wall_m swept knob; per-ring sum > old average; knuckles counted; airborne mass jumps | MIN_TUBE_WALL_M/tube_wall_thickness/ring_beam_mass/knuckle_mass_at_ring (L52-105; defs trpt_optimization.jl:32,75,88,59); live decode+build on the island-1 winner genome (L37-47); expansion_airborne_mass (L110) | runtests:55 | **LIVE** — exactly reproduces objective_evaluator.jl:346-375 mass accounting on the real campaign winner |
| 26 | test_metric_consistency.jl | SimFrame metrics == direct recomputation; grep-guard against inline k·ω³ | metric consistency; grep guard for inline power formulas | params_10kw build, settle_to_operational_state, run_canonical_sim!, capture_frame, get_generator_torque, get_max_rope_tension (L8-74); scripts text scan (L77-105) | runtests:42 | **PARTIAL** — kernel-consistency checks run on live capture/sim code but on a legacy machine; grep-guard half is pipeline-wide (incl. v13 scripts) |
| 27 | test_objective_v11.jl | Gate-3 v11 objective: fitness units, warmstart A1/A2, feasibility tiers | v11_fitness ×5; objective_v10 static pin; objective_feasibility ×4; n_lines decoder ceiling; Betz rows; util-split identity; with_k_bracket | v11_fitness (L35-81); objective_v10 + params_v5_50kw (L99); objective_feasibility (L110-176); design_from_vector_v4 (L184); objective_v11_warmstart→evaluate_windowed warm (L204,230); with_k_bracket (L257) | runtests:50 | **PARTIAL** — v11_fitness/objective_feasibility/objective_v10-static = legacy scoring/solver (no v13 callers); but A2/A1 warmstart testsets + with_k_bracket + decoder-ceiling test exercise the shared evaluate_windowed protocol and decode clamps the v13 chain also uses |
| 28 | test_objective_v12.jl | V12 power-window fitness units + warmstart smoke | v12_fitness window/FoS/hard-gate/knobs/cap; objective_v12_warmstart smoke; mass_min_fitness | v12_fitness + mass_min_fitness + ObjectiveConfig (L12-109); objective_v12_warmstart→evaluate_windowed warm on params_v5_50kw (L69-73) | runtests:51 | **PARTIAL** — v12_fitness is legacy (v12-era adapter, last real caller ramp_calibration_campaign.jl); mass_min_fitness is a live seam fn (diag scripts) sharing guards with the campaign's appropriate_mass_fitness; warmstart smoke runs the live shared protocol but with the legacy v12 scorer + 50 kW params |
| 29 | test_parameters.jl | (no header) trivial params sanity | parameters | params_10kw n_rings=14, n_lines=5 (L2-7) | runtests:16 | **LEGACY-ONLY** — params_10kw is the DRR theory anchor (parameters.jl:263); the v13 chain anchors on params_daisy (parameters.jl:338) |
| 30 | test_physics_inertia_mass.jl | Gate 2b blade inertia rod-integral + unified volume law | inertia rod-integral; Daisy plausibility; mass span³; blade_scale 0.85 | expansion_rotor_inertia (L22-46; live caller initialization.jl in ODE build), expansion_blade_mass (L41-73; live via builders_util.jl:106), physics toggles | runtests:45 | **LIVE** — inertia fn is used building v13 ODE systems; blade-mass law matches the live er.mass construction |
| 31 | test_physics_path_guard.jl | static objective must differ between LEGACY/DEFAULT physics | physics-path guard | objective_v10 + params_v5_50kw + set_expansion_physics! toggles (L33-46) | runtests:52 | **LEGACY-ONLY** — guards the *static* objective_v10 solver, which no v13 entry point calls (v13 scores through the ODE evaluate_windowed); a physics decoupling in the ODE path would not trip it (see §4) |
| 32 | test_pitch_depower_sequence.jl | pitch-depower sequences + compliant winch | pitch depower sequence and compliant winch | params_10kw build, settle_to_operational_state, run_pitch_depower! (L2-45) | runtests:40 | **PARTIAL** — run_pitch_depower!/settle are live shared controllers; fixture machine is legacy 10 kW |
| 33 | test_power.jl | torsional power propagation to ground ring | power generation | params_10kw build, settle_to_equilibrium, simulate (L4-26) | runtests:35 | **PARTIAL** — simulate/settle are live kernels; legacy fixture (params_10kw machine; v13 measures P at the ground ring via τ_gen·ω_gnd instead) |
| 34 | test_ring_element_analysis.jl | per-beam ring element structural analysis | Tests 1-10: N_crit 4×; symmetric→uniform N; asymmetric; OOP; self-equilibration; gravity+drag; moment/torque equilibration; ground_station_forces; OOP relaxation; F4b sys-ref taper law | ring_element_analysis/analyse_ring/assemble_ring_frame/solve_ring_frame/extract_beam_forces/tube_props/ground_station_forces + strut_properties (L10-40,118,132,198,295-329) — analyse_ring def ring_element_analysis.jl:440, consumed by the live capture_extended (sim_frame.jl:381) | runtests:38 | **LIVE** — this module IS the v13 FoS authority (evaluate_windowed samples ef.ring_fos via capture_extended every second); fixtures are legacy-built but Test 10 (L255-329) pins the sys-ref vs design analyse_ring branches the evaluator populates (objective_evaluator.jl:446-456) |
| 35 | test_ring_forces.jl | ring force kernel incl. sky-anchor loading | ring forces | params_10kw build, compute_ring_forces! + rotary_lifter_default (L4-32) | runtests:29 | **PARTIAL** — compute_ring_forces! is the live v13 torque source; legacy fixture + legacy lifter |
| 36 | test_ring_spacing_v4.jl | v4 constant-L/r spacing law + v4 static design solver | constant L/r; monotonicity; boundaries; ground-ring constraint; torsional-FoS uniformity; v4<v2 spread; cylindrical; max_rings=1; bounds + design_from_vector_v4; mass accounting | ring_spacing_v4 (L19-52; live), TRPTDesignV4 + evaluate_design static (L59-92,184; def ring_spacing.jl:311 — **not** the ODE FoS path), TRPTDesignV2 evaluate_design (L108-115; trpt_axial_profiles.jl:214), design_from_vector_v4/objective_v4/search_bounds_v4 (L156-175), params_10kw fixtures | runtests:37 | **PARTIAL** — the spacing-law testsets (1-3,7) pin live geometry (decode = design_from_vector_v4 alias via objective_v5.jl:31; warm path ring_spacing_v4 objective_evaluator.jl:576); testsets 4-6,8-9 pin the v4 *static FoS solver* + objective_v4, which no v13 code calls (v13 FoS = ring_element_analysis) |
| 37 | test_rope_break.jl | acceptance R1-R3 rope break (SK99 3.5%) | R1 line break criterion; R2 pre-fix winner bounded/broken; R3 seed healthy band | campaign build_from (decode+build_system_from_v10, L48-59), settle_to_operational_state, run_canonical_sim! any_broken latch, stable_dt_for_system (L134) — note R1/R2 lift via rotary_lifter_default (L69,98) | acceptance list | **LIVE** — v13-built machines + live rope-break latch the evaluator also gates on (objective_evaluator.jl:761) |
| 38 | test_rope_forces.jl | rope force kernel at rest | rope forces | params_10kw build + compute_rope_forces! (L4-16) | runtests:28 | **PARTIAL** — live kernel, legacy fixture |
| 39 | test_rope_sag.jl | gravity sag of rope segments | rope sag in zero wind | params_10kw build, settle_to_equilibrium, attachment_point/shaf t_perp_basis (L4-29) | runtests:32 | **PARTIAL** — live settle kernel; legacy machine |
| 40 | test_rotor_power_realism.jl | acceptance P1-P4 rotor realism | P1 cp falloff; P2 drag brake cubic; P3 per-rotor Betz helper; P4 no freewheel | cp_at_tsr (L25-46), rotor_betz_ok (L53-60; def objective_evaluator.jl:224), params_10kw-scaled P4 machine on the v13 decode+ODE (L67-91) | acceptance list | **LIVE** — pins table rolloff + the per-rotor Betz gate the evaluator applies in-window (objective_evaluator.jl:725,858); P4 fixture is a legacy-base artifact but runs the live ODE path |
| 41 | test_settle_blocking_2026_09.jl | settle scan must apply per-rotor wake de-rate | wind factors populated; helper de-rates hub+middle; single rotor un-de-rated | settle_aero_power (L72-87; def initialization.jl:849, used in the settle scan L942), live decode+build, compute_seeds constants | runtests:56 | **LIVE** — pins the cold-settle power scan the v13 cold path runs (objective_evaluator.jl:623) |
| 42 | test_settle_drag_alignment.jl | acceptance A-E settle/drag alignment | B bit-identity zero drag; E drag sanity; A gap reduction; C monotonicity in tether d; D no new stall | campaign build (L47-57), settle_to_operational_state + drag_fn, settle_parasitic_drag_power (L92-132), winner CSV, run_canonical_sim! (L109) | acceptance list | **LIVE** — v13 campaign seed/winner through the live settle+ODE path |
| 43 | test_settle_lowk_honest.jl | acceptance A1-A4 low-k honest telemetry | A1 k=0.5; A2 k=2.0; A3 k=5.39; A4 settle scan cp-peak clamp | evaluate_windowed cold + appropriate_mass_fitness at 3 k values on the campaign seed (L60-79), cp_at_tsr settle-clamp reproduction (L120-142) | acceptance list | **LIVE** — the campaign eval path incl. the honest-reject ObjectiveResult contract (objective_evaluator.jl:915) |
| 44 | test_spacer_ring_design.jl | SpacerRing structural design module (strut buckling/utilisation) | Material presets; circular/elliptical/airfoil struts; utilisation & buckling margins; legacy adapter | strut_properties/utilisation/CircularTube/DEFAULT_CFRP/tube_props (L7-125) | runtests:39 | **LIVE** — this module is the ring-beam engine analyse_ring itself calls (ring_element_analysis.jl:502-520), hence the v13 per-ring FoS/M_el; test constructs no machine but asserts the engine's math |
| 45 | test_static_equilibrium.jl | settle leaves hub aloft, no NaN, velocities ~0 | static equilibrium | params_10kw build, settle_to_equilibrium (L4-6) | runtests:31 | **PARTIAL** — settle kernel live; legacy fixture |
| 46 | test_trpt_axial_profiles.jl | v2 axial r(z) profile family + 12-DoF design plumbing (cartography Phase A) | axial profile family; TRPT v2 geometry; v2 evaluate_design; v2 bounds+decode; n_lines affects polygon FoS | r_of_z/axial profiles (L14-42); TRPTDesignV2 baseline_design_v2/evaluate_design/design_from_vector_v2/objective_v2/search_bounds_v2 (L47-113; defs trpt_axial_profiles.jl:119-386), params_10kw | runtests:36 | **LEGACY-ONLY** — the whole v2 design stack predates v4/v10 (scripts run_trpt_optimization_v2/run_v6_cartography era); no v13 code path reaches TRPTDesignV2/r_of_z |
| 47 | test_types.jl | node counts/state layout of a built system | types and node counts | params_10kw + build_kite_turbine_system + state_size (L2-19) | runtests:26 | **PARTIAL** — node/state invariants hold for the shared node types the v13 ODE also uses, but the asserted 243-node/16-ring layout is the legacy 10 kW machine, not any v13 build |
| 48 | test_wind_blocking.jl | co-axial wake blocking decode + clearance authority | decode direction; rotor ordering; radii for clearance; lowest_rotor_clearance absolute tip; bank swings tip; 3 legacy-constructor defaults; wind_factor propagation | design_from_vector_v10 rotor_count_mode + blocking (L31-112), lowest_rotor_clearance (L90-112; def objective_v10.jl:321 — the runner's clearance gate), expansion_params_from_rotors wind_factor (L136-141) | runtests:49 | **LIVE** — pins the exact decode knobs/constants the v13 runner and gate use (run_v13_5kw_masslift.jl:259-263) |

## 2. Current v13 pipeline call chain (evidence-cited) + zero-live-caller inventory

### 2.1 Campaign chain (entry scripts)
1. `scripts/run_v13_5kw_masslift.jl` (DE runner, cold start): includes compute_seeds.jl (:27); `lift_for = sized_lifter_for(sys,p; margin=1.5, v_ref=11.0, const_tension=true)` (:90-91); `params_at_length(L)` = params_daisy() → spec structs → `mass_scale(p,1.5,5.0)` → `override_params(tether_length=L)` (:134-148); cfg = ObjectiveConfig(:power_W=5000, p_floor=ceiling=5, fos 2.5/2.5, :tail5, k=K_MPPT_5KW_HONEST, rotor_count_mode=true, power_split=0.6, cone 22°, spacing 0.8, blocking=BLOCKING_WIND_FACTOR_5KW, min_wall_m) (:161-186); per-eval: `design_from_vector_v10(x, beam, p_base; cylinder_cone=true, rotor_count_mode=true, ...)` (:259-262) → `lowest_rotor_clearance` gate (:263-266) → `evaluate_windowed(…; start_mode=:cold, lift_device=lift_for, fitness_fn=appropriate_mass_fitness)` (:270-276). Fitness value = mass + 3 penalties (see 2.3).
2. `scripts/ode_gate_v13.jl`: same lift_for (:36-37); params_at_length (:39-54); `gate_design(x)` (:86-168): decode with the runner's knobs (:102-105) → clearance (:109-110) → `build_system_from_v10(dec, 1.0, k_mp; tether_diameter, base_params=p)` (:112-113) → `stable_dt_for_system` (:119) → `settle_to_operational_state(…, 60.0; n_op=30_000)` (:123) → 10 s relax via `run_canonical_sim!` (:137-139) → 30 s windowed chunks (:144-154), reading `P_gen = τ_gen·ω_gnd` from `get_generator_torque` (:149-150), per-segment `twist_report` (:151), `tip_speed_sanity_ok` (:209); rejects on rope-break latch `sys.any_broken[]` (:161-162).
3. `scripts/compute_seeds.jl`: single-source constants K_MPPT_5KW_HONEST=2.24 (:34), BLOCKING_WIND_FACTOR_5KW=0.75^(1/3) (:41); `seed_genome` (:61), `tight_bounds` (:101).
4. `scripts/smoke_masslift_v13.jl`: runner path replicated; additionally verifies in-run T_lift vs 1.5·m_airborne·g/sin70° using `expansion_airborne_mass(sys,pc; include_lifter=false)` on a fresh decode+build (:65-84).
5. `scripts/regate_winner_v13.jl`: includes ode_gate_v13.jl and calls `gate_design` (:16) — inherits the whole chain.
6. `scripts/combine_islands_v13.jl`: CSV-only, no module code.
(Post-analysis `analyze_campaign_winners.jl` is a k-constant consumer per test_campaign_k_alignment.jl:18-23 but is not on the DE/eval chain.)

### 2.2 evaluate_windowed protocol (src/objective_evaluator.jl)
`evaluate_windowed` (:482) → length check vs TRPT_V10_DIM=14 (:501) → `design_from_vector_v10(x14, beam, p; power_W, v_rated, cylinder_cone=true, rotor_count_mode, power_split, cone_slope_deg, rotor_spacing_frac, blocking_factor)` (:504-509) → gates n_active/n_rings/spacing (:510-523) → `build_system_from_v10(result, 1.0, k_mppt; tether_diameter, base_params=p, min_wall_m)` (:533-534) → `stable_dt_for_system` (:541) → `rotor_annulus_ok` (:545) → lift_dev = `lift_for(sys, pc)` (:556) → cold branch: `settle_to_operational_state(sys,u0,pc,60.0; wind_fn, lift_device, n_op=30_000)` (:623; def initialization.jl:882, internally uses `settle_aero_power` scan :942) → relax+window `run_canonical_sim!` with per-second callback (:749-751) that samples `twist_collapse_check` (:692), `tip_speed_sanity_ok` (:700), `capture_extended` (:703; def sim_frame.jl:360 → `ring_element_analysis` :381 → `analyse_ring`, def ring_element_analysis.jl:440 → strut_properties ring_element_analysis.jl:502-520), per-rotor Betz via `rotor_betz_ok` (:725) → post-run hard gates: rope break (:761), twist (:781-784), hub divergence (:787), Betz flag (:792) → FoS_min via `min_airborne_fos` (:799-807) → annulus-area Betz ceiling P_mean ≤ 1.1·0.593·½ρv³·A_total (:841-860) → score seam `fitness_fn(P_score(F_end tail5), FoS_min, cfg, m_airborne=expansion_airborne_mass(sys,pc))` (:895-906) → honest-reject ObjectiveResult when fitness non-finite (:907-917) → stationarity + W_TWIST_KG over-twist penalties (:919-934).
`build_system_from_v10` (:322): `expansion_params_from_rotors(rotors, n_rings, n_lines; blade_scale)` (:343; def builders_util.jl:79 — also used by the warm path :574) → per-ring `ring_beam_mass` + `knuckle_mass_at_ring` sums over decoded radii (:355-375; defs trpt_optimization.jl:88,59) → hub blade m_blade = M_BLADE_REF_KG·span_hub³ with span = decoded tip−hub offset (:413-416; M_BLADE_REF_KG = 0.420 parameters.jl:18) → `build_kite_turbine_system_v5(pc, target_Lr, r_bottom; density_profile, taper_start_z, harvest_length, expansion_rotors, rotor_blade_hub_radius, main_rotor_wind_factor)` (:428-436; def initialization.jl:362, ring geometry from `ring_spacing_v5` initialization.jl:376) → ring beam refs + mass totals written to sys refs (:446-456).
Decode `design_from_vector_v10` (objective_v10.jl:129): base design via `design_from_vector_v5` (= `design_from_vector_v4`, alias objective_v5.jl:31; def ring_spacing.jl:469) (:144) → rotor placement rotor_count_mode {:1,2,3} or legacy bitmask (:149-157) → three-section geometry via `ring_spacing_v5` (:184; def ring_spacing.jl:149) → per-rotor `BEM.rotor_radius_for_power` (:237,267; BEM in bem.jl) → 70/30 span/blade offsets (:269-284) → wind_factor = blocking for downstream rotors (:262).
Fitness (objective_v12.jl): `appropriate_mass_fitness` (:145) = mass + W_OVERPOWER·max(P−ceiling,0)² + W_TWIST·(r/margin)² + W_UTIL·(fos_hard/FoS)², hard-rejecting non-finite/FoS<fos_hard and P<p_floor (:149-167); weights :114-116; same W_TWIST term is applied live to every objective at objective_evaluator.jl:931-933.
Physics knobs on the ODE path: `sys.rotor.wind_factor`/`er.wind_factor` de-rate inflow inside compute-ring/rope force kernels (ring_forces.jl:197,279) — set from decode via expansion_params_from_rotors (builders_util.jl:79) and evaluator (:399); ground-side power = τ_gen·ω_gnd via `get_generator_torque` (sim_frame.jl exports, gate ode_gate_v13.jl:149).

### 2.3 Builder functions & params sets with ZERO callers on the v13 chain (dead/legacy-only) — evidence
- `build_v10_tight`, `build_v10_tight_no_lowest`, `_build_v10_tight`, `build_phantom_triangle`, `geometry_fingerprint` (builders_util.jl:24,36,119,253,350): **no v13 caller**. Callers are legacy scripts (run_v10_campaign-era gates/diags/sweeps, e.g. gate2.jl, discriminator.jl, daisy_builder.jl, kickstart/wind sweeps — full list from grep), src/control_map_hunt.jl (which is itself **not included** in the module, KiteTurbineDynamics.jl:7-47 include list; only legacy scripts use it), and tests. Only `expansion_params_from_rotors` (builders_util.jl:79) is live (evaluator :343,:574).
- `solve_ring_Do` (trpt_optimization.jl:108 — named in the HEAD commit): **zero callers anywhere** in src/scripts/test (grep: only its own def + docstring at trpt_optimization.jl:95,108). Unwired and untested despite being the HEAD feature.
- v1 `TRPTDesign` static optimiser stack in trpt_optimization.jl: `evaluate_design`(:772), `baseline_design`(:832), `search_bounds`(:862), `design_from_vector`(:908), `objective`(:930), `beam_section_properties`(:240), `peak_hub_thrust`(:303 — only legacy caller objective_v10.jl:478). Reachable only from legacy scripts (run_trpt_baseline.jl, run_trpt_optimization.jl, overlay_designs.jl…) and test_golden_traces; no v13 caller. (Live in the same file: mass model :25-190.)
- v2 `TRPTDesignV2` stack (trpt_axial_profiles.jl:119-407): `r_of_z`/profiles, `design_from_vector_v2`(:348), `objective_v2`(:372), `baseline_design_v2`(:386), `evaluate_design`(:214), `search_bounds_v2` — legacy-only (v2 cartography era scripts + tests).
- v4 *static* solver (ring_spacing.jl): `evaluate_design(::TRPTDesignV4)`(:311), `objective_v4`(:501), `baseline_design_v4`(:522), `search_bounds_v4`(:415) — legacy-only callers (objective_v10.jl:513 static path, run_v4_campaign.jl era scripts, tests). **Live in the same file:** `ring_spacing_v4`(:60 — warm-path eval, decode base), `ring_spacing_v5`(:149 — ODE decode + build), `design_from_vector_v4`(:469 — the decode's base via the v5 alias).
- Objective adapters, all zero v13 callers: `objective_v5` (objective_v5.jl:33 — run_v5* campaign scripts), `objective_v6` (objective_v6.jl:615 + `estimate_effective_radii` :149 — run_v6_campaign era), `objective_v10` static (objective_v10.jl:375 — run_v10_campaign.jl/recampaign/run_anchor_batch/stage1b), `objective_v11`/`objective_v11_warmstart` (objective_v11.jl:88,125 — v11-era diag scripts; v12 file doesn't call them), `objective_v12`/`objective_v12_warmstart`/`warmstart_with_k_bracket_v12`/`objective_v12_ramp` (objective_v12.jl:181-284 — last real caller ramp_calibration_campaign.jl), `objective_feasibility` (objective_feasibility.jl — scripts run_feasibility_phase_a.jl/rescore_phase_a.jl + tests only). The **shared protocol under them (evaluate_windowed, with_k_bracket, gates) is live** — only the version seams and their scorers are legacy.
- Scorer functions with no v13 caller: `v11_fitness`, `v12_fitness` (v13 chain uses `appropriate_mass_fitness`; `mass_min_fitness` survives only in diag scripts + tests), plus `warmstart`-only helpers. `mass_min_fitness` appears in run_v13_5kw_masslift.jl/ode_gate_v13.jl only in comments (run_v13_5kw_masslift.jl:159; ode_gate_v13.jl:27).
- Params sets with no v13 reachable caller: `params_10kw` (parameters.jl:263), `params_50kw`(:398), `params_v5_10kw`(:420), `params_v5_50kw`(:453), `params_v6_50kw`(:464), `params_v5_safe_10kw`(:513) — the v13 chain always passes a daisy-derived base (evaluator :533-534); params_v5_50kw survives only as the no-base_params default inside build_system_from_v10 (:336), which no v13 caller hits. **Live:** `params_daisy` (:338 — runner/gate/smoke/acceptance), `mass_scale` (:558), `override_params` (simulation.jl:260), `M_BLADE_REF_KG` (:18).
- `decode_rotor_mask`/VALID_ROTOR_MASKS bitmask branch (objective_v10.jl:68,59): the v13 chain always sets rotor_count_mode=true (evaluator :506; runner :260), so the 60-mask decode is legacy — but still exercised by the static objective_v10 and older scripts/tests.
- `build_kite_turbine_system` (v1 linear-taper builder, initialization.jl): no v13 caller (build_system_from_v10 uses `build_kite_turbine_system_v5` :428); still used by headless_verify.jl, control_map_hunt.jl and many legacy scripts. `build_kite_turbine_system_v5` is the live ODE builder.

## 3. Plain-language invariants for the four requested files

**test_builders_v10.jl** — asserts about the *old tight builder* (build_v10_tight_no_lowest, builders_util.jl) and the *decode↔builder mapping*:
- 13-gon machine: n_lines 13, n_rings 10, r_hub 2.889 m, r_bottom 2.0 m (r_hub > r_bottom), tether 67.08 m (L10-21) — v10 50 kW campaign artifact (v10_campaign_50kw/best_vector.csv). Round-trip: decode of that CSV must reproduce the tight builder's design field-for-field (L23-40).
- r_bottom_scale applied once (2.0·1.30 = 2.60, never squared) and never above r_hub (L42-49); blade_scale must not move ring geometry (L51-58); drop-direction removes exactly one expansion rotor (L60-70); n_blades = n_lines flows through (L72-77).
- **Live mapping invariants (matter for v13):** rotor→system ring numbering is identity with the hub (top ring) excluded from expansion rotors — no +1 off-by-one (L83-105); blade_scale multiplies both decoded tip and hub offsets (L107-113); minimal machine = hub-only (L115-132); a rotor_count_mode 3-rotor Daisy-scaled genome places hub at ring n and expansions at n−1, n−2 in the built system (L134-164). These are the same mapping the v13 evaluator relies on (objective_evaluator.jl:343,384-396).

**test_blade_geometry.jl** — asserts the 70/30 ring-anchored blade split on **tight-builder expansion rotors** (L13-53) and ExpansionStackConfig defaults (L56-70): blade_tip_radius > 0 (outboard +0.7·span), blade_hub_radius < 0 (inboard −0.3·span), and the exact ratio tip/(−hub) = 7/3 for the whole span range and for blade_scale ∈ {1.0, 0.69}, r_bottom_scale 1.30, tether 4 mm variants. **Relevance to v13:** the same convention is applied in the live decode (objective_v10.jl:269-284: span=0.75·r_rotor·blade_scale, tip=+0.7·span, hub=−0.3·span) and produces the annulus the ODE sweeps (expansion_annulus_area expansion_rotor.jl:197; evaluator Betz gates :713-721, :841-847) — so the invariant still matters, but this file would not catch a regression inside design_from_vector_v10 (only test_builders_v10.jl:107-113 and the decode-asserting testsets do).

**test_ring_spacing_v4.jl** — two layers:
- Spacing law (L15-53,128-151): ring_spacing_v4 places rings so every segment has L/r ≈ target_Lr (geometric series in radius; <0.1% spread, ≤2% from target after integer rounding); z and r strictly increase ground→hub; endpoints z=0/L, r_bottom/r_top exact; cylindrical (r_top=r_bottom) degenerates to uniform z-spacing; max_rings=1 gives the minimal 2-ring system. **Still live:** v13 geometry derives from this same law — decode base design (design_from_vector_v4 via alias) and the static warm-start ring radii (objective_evaluator.jl:576), with ring_spacing_v5 the cylinder+cone generalisation (objective_v10.jl:184, ring_spacing.jl:149).
- Static TRPTDesignV4 solver (L56-93,96-125,154-188): r_bottom > OPT_MAX_GROUND_RADIUS ⇒ evaluate_design infeasible with the deployment-constraint message; with constant L/r + Do∝r^0.5, buckling *and* torsional FoS are uniform across rings (<10% spread at ω=0) and tighter than the v2 uniform-spacing profile's spread; design_from_vector_v4/search_bounds_v4/objective_v4 plumbing; mass accounting (beams/knuckles > 0). **Not live for v13:** v13 FoS is measured in the ODE window by ring_element_analysis/analyse_ring (sim_frame.jl:381), not by this static evaluate_design; these testsets only guard the frozen static solver used by objective_v10-era scripts. params_10kw is used throughout as the fixture (DRR-era).

**test_geometry.jl** — pure-helper invariants (no builder): shaft_perp_basis returns a unit, mutually-orthonormal pair for a 30° shaft (L3-15); attachment_point lies exactly R from the ring centre and the n_lines=5 points are equally spaced by 2π/5 (L17-33); rope_helix_pos interpolates attachment A→B with fraction 0/1 exactly at the endpoints (L35-39). **Relevance:** these helpers generate rope attachment/helix geometry for every ODE build including v13's (compute_rope_forces!, initialization geometry); they are builder-agnostic and fully live — mass/tension conventions are NOT asserted here (that lives in test_blade_mass_law/test_fitness_appropriateness/test_rope_break).

## 4. False-confidence flags (green but asserting code the v13 chain never calls)

1. **test_physics_path_guard.jl** — green ⇒ "static objective still honours physics toggles". The v13 pipeline scores exclusively through evaluate_windowed's ODE path; objective_v10 (static solver) has no live caller. A refactor that silently decoupled the *ODE* force path from the physics flags would pass this guard untouched. The warm/cold ODE paths' expansion-physics toggles are not asserted by any test.
2. **test_blade_geometry.jl** — fully green against build_v10_tight_no_lowest/build_expansion_stack (both legacy). A 70/30 regression introduced only in design_from_vector_v10/build_system_from_v10 would leave it green; the actual v13-split pins live in test_builders_v10.jl (L107-113, L134-164), test_blade_mass_law.jl (L42-77) and test_wind_blocking.jl.
3. **test_builders_v10.jl (first 7 testsets)** — green against the tight builder and the v10_campaign_50kw artifact (params_v5_50kw decode, r_hub 2.889, r_bottom 2.0, tether 67.08 m, 13-gon). None of those machines or builder calls exist in the v13 chain (which is Daisy-anchored, 18.8 m, rotor_count_mode). The file's last testsets are live; the first half is a museum pin.
4. **test_objective_v11.jl / test_objective_v12.jl / test_fos_guard.jl** — the v11_fitness / v12_fitness / objective_feasibility / objective_v10-static / fos-cap assertions are all against legacy scorers and the static solver. Only the warm-start-smoke, k-bracket, decoder-ceiling and honest-guard portions touch shared live code, and even those run with params_v5_50kw + 50 kW defaults, not the campaign's daisy base. **test_fos_guard.jl is the sharpest gap:** it would stay green if the non-finite-FoS guard were deleted from appropriate_mass_fitness only (its v12/v11 siblings still guard).
5. **test_documented_claims.jl** — green proves v10-era numbers (2.889 m dodecagon, params_10kw m_blade = 11/5 kg, v6.2 74.17 kg, 167.474 kW legacy row) are unchanged; none constrain the v13 machine (its anchors — 420 g blade, 1.52/2.22 m tips, k 2.24 — are asserted in test_blade_mass_law.jl:19-21, test_campaign_k_alignment.jl:40, test_mass_model_2026_09.jl, and the acceptance files).
6. **test_trpt_axial_profiles.jl / test_expansion_stack.jl / test_parameters.jl** — entirely green against v2-design / build_expansion_stack / params_10kw layers with zero v13 callers (the v2 family also has no live *script* callers — last use was the v2/v6 cartography era).
7. **Softer:** the params_10kw-based ODE/structural files (test_dynamics, test_rope_forces, test_ring_forces, test_static_equilibrium, test_power, test_rope_sag, test_emergent_torsion, test_dashboard_smoke, test_metric_consistency, test_pitch_depower_sequence, test_bearing_alignment, test_types, test_geometry-siblings) exercise genuinely live kernels (compute_rope/ring_forces!, multibody_ode!, settle_*, run_canonical_sim!, run_pitch_depower!, apply_brake_constraint!, capture_frame) but always on the legacy 10 kW machine (n_lines 5, 16 rings, linear-taper r_bottom from trpt_rL_ratio). They would not notice the v13 geometry conventions (70/30 annuli, ring-anchored mass, wind-blocking, ground-ring power) regressing; e.g. none of them would catch a regression that only affects machines with ring-anchored hub blades (rotor.blade_hub_radius > 0), because build_kite_turbine_system builds full-disk legacy rotors.

### Net read
- 21 files fully LIVE (1-2,4,6,8,13,15,18,20-21,24-25,30,34,37,40-44,48) — dominated by the 2026-08-20→09-07 v13-era work.
- 18 PARTIAL (3,7,9,11-12,14,16,19,22,26-28,32-33,35-36,38-39,45,47 — the bulk being shared-kernel tests on legacy machines).
- 8 LEGACY-ONLY (5,10,17,23,29,31,46 + test_parameters) — of these, 5/10/23/46 are intentional provenance/era pins with documented value; 31 (physics-path guard) and 5 are the ones most likely to mislead about v13 health.
- False-confidence hotspots to fix cheaply: add an appropriate_mass_fitness non-finite-FoS case to test_fos_guard.jl; add an ODE-path physics-toggle guard (or extend test_physics_path_guard to evaluate_windowed cold); consider a decode-built 70/30 assertion in test_blade_geometry.jl so it guards the live split.
