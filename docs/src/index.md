# KiteTurbineDynamics.jl

Multibody dynamics, structural safety, and economic sizing for Tensile Rotary
Power Transmission (TRPT) airborne wind energy kite turbines.

This site is generated with [Documenter.jl](https://documenter.juliadocs.org/).
It is a thin, auto-generated reference over the package's public surface; the
authoritative narrative documentation lives elsewhere in the repository:

- **Project overview & run instructions** — see the repository
  [`README.md`](https://github.com/OWNER/KiteTurbineDynamics.jl#readme).
- **Domain context** — `CONTEXT.md`.
- **Design & optimization decisions** — `DECISIONS.md`.
- **Architecture decision records** — `docs/adr/`.
- **Contributor & agent guide** — `AGENTS.md` and `CLAUDE.md`.

## Installation

```julia
julia> using Pkg

julia> Pkg.develop(path = ".")    # from a clone of the repository
```

## API reference

See the [API](@ref) page for the documented exported symbols. Coverage is being
filled in incrementally; the build currently runs with `warnonly = true` so
undocumented exports do not fail the docs job.
